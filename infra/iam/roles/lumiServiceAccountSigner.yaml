title: Lumi Service Account Signer
description: Sign Firebase custom tokens and short-lived Cloud Storage URLs as the bound runtime identity.
stage: GA
includedPermissions:
  - iam.serviceAccounts.getOpenIdToken
  - iam.serviceAccounts.signBlob
