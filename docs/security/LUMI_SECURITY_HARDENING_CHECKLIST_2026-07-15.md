# Lumi Security Hardening — Plan & Checklist

**Merged:** 2026-07-15 · combines the detailed technical audit + the plain-English summary into one working document.
**Purpose:** a single source of truth that a later session (or you) can run through top-to-bottom, ticking `[x]` as each fix lands and its **Verify** step passes.
**Scope reviewed:** Flutter app, active `firestore.rules` + `storage.rules`, Cloud Functions, school portal, super-admin portal, repo config, tests. Source-code + config only — the *live* deployed state (rule versions, App Check, IAM, backups, budgets) can't be proven from the repo and is captured in the "Verify outside the repo" section.

---

## How to use this checklist

- Work **P0 → P1 → P2**. Don't tick an item until its **Verify** line passes (a test, an emulator run, or a console check).
- Each item has: **Severity**, **Why it matters** (plain English), **Where** (file:line), **Fix** steps, **Verify** steps.
- `[ ]` = todo, `[x]` = done + verified, `[~]` = in progress / partially done.
- The **New negative-test matrix** near the bottom lists the automated tests that must exist and pass — most fixes should add one or more of them.

## Implementation log / session handoff

### 2026-07-15 — hardening implementation started

- **Current item:** Public Google/Firebase/Twilio/OAIC contract evidence is retained with hashes, the APP 8 lawyer brief is ready, incident delivery/operational roles are recorded, and the three application secrets have been migrated to Sydney-only replicas with verified live consumers and deleted global predecessors. Remaining P2-3 work is private account-acceptance evidence, an appointed Privacy/Legal Lead, school emergency contacts and owner/counsel APP 8/PIA approval. Remaining P2-5 evidence is genuinely old/low-end hardware, the iOS App Store URL and a store-signed force-update canary; the physical ISBN plus student/account-deletion paths have passed. Remaining P1 gates are documentary/store evidence: each enabling school's real-world authority/notice process, live privacy questionnaires, store-attested App Check rollout, Play signing identity and final store-signed artifact scans.
- **Status:** P0-1 through P0-8, P1-2, P1-4, P1-5, P1-6, P1-8, P1-9, P2-2, P2-4 and P2-6 are complete; P1-1/P1-3/P1-7/P2-1/P2-3/P2-5 are partially implemented. P1-1's non-store controls are complete. P2-4 now has stable 30-record mobile pagination plus server-owned, transaction-safe eight-shard class/day summaries, a weekly exact reconciler, Rules/index coverage and a production backfill. Teacher calendar, weekly chart, hero intelligence and home-widget paths use summaries; only optional current-week sentiment/group detail retains a bounded raw-log query. All earlier access, audio, offline, privacy-default, aggregation, backup, monitoring and dependency controls described below remain in force.
- **Working rule:** changes are being made in checklist order; each completed checkbox will include the verification command/result here or under the item.
- **Latest 17 July regression:** Functions **145/145**, Firestore Rules **156/156**, Storage Rules **16/16**, audio integration **8/8**, summary integration **2/2**, dashboard scale **2/2**, focused pagination/summary Flutter **20/20**, focused cover scanner **10/10** and error-path Flutter **96/96** passed. Functions build/lint has 0 errors (8 existing warnings); changed-file Flutter analysis is clean. Full Flutter analysis has no errors/warnings (**122 existing info notices**). Both portals use Firebase-compatible Next.js **15.5.20** and Firebase Admin **13.10.0**; both production builds and live IAM capability canaries pass. The combined portal production audit has **0 critical/high** findings (5 moderate, 1 low), and the separate Functions production audit has **0 critical/high** findings (8 moderate).
- **Baseline before edits:** Firestore rules 125/125, Storage rules 6/6, Functions 109/109, targeted Flutter offline/log tests 40/40. Functions dependency audit: 2 critical, 7 high, 12 moderate, 1 low.
- **Verification so far:** `cd functions && npm run test:functions` → **141/141 passed**; `npm run test:rules` → **152/152 passed**; `npm run test:rules:storage` → **14/14 passed**; `npm run test:audio:integration` → **8/8 passed**; `npm run test:audio:http` → **4/4 passed**; `npm run test:audio:appcheck` → **1/1 passed**; `npm run test:deletion:integration` → **2/2 passed**; targeted Flutter reading/offline/audio tests → **47/47 passed** and the P1 offline/SMS/linking suite → **84/84 passed**. Real-media tests fully decode a valid M4A and reject both a truncated `ftyp` header and a 62-second file. Functions build and lint pass (8 existing warnings, 0 errors), and the Functions production dependency audit has **0 critical/high** advisories (8 moderate; the remaining `uuid` chains are not called with attacker-controlled input). Earlier targeted Flutter service/screen suite → **50/50 passed**, onboarding/callable migration suite → **28/28 passed**, mobile dev-access suite → **2/2 passed**, portal session-policy + audio-authority suite → **4/4 passed**. Both portal Next 15.5.20 production builds succeed; a frozen root workspace install and combined production audit pass with **0 critical/high** advisories (5 moderate, 1 low). The live school canary passes Firestore/Auth/upload and the live super-admin canary passes session-cookie Auth, Firestore, bucket and self-scoped signing. `flutter analyze --no-fatal-infos` → no errors/warnings (**122 existing info notices**). The broad Flutter run reports **400 passed / 8 unrelated UI-test failures**: stale finders in feelings tracker, splash, teacher assignment card, comment chips and week progress bar, plus a pending toast timer in the awards test. Fresh Android release APK and unsigned iOS application builds succeed; the corrected package scanner inspected **1,105 extracted APK files**, and Gitleaks history over **612 commits** plus available mobile artifacts is clean. Live API-key negative probes reject an unlisted web referrer, iOS bundle and Android signing certificate while accepting every registered Lumi identity; all three current Firebase keys retain their 27-service API allowlist. `git diff --check` and the redacted Gitleaks working-diff scan are clean.
- **Resume point if interrupted:** IAM, P2-1, P2-4 and P2-6 are complete; do not restore default-account `Editor` or Scheduler use of the App Engine default account. Production has 74 Functions on `lumi-functions-runtime`, one school portal on `lumi-school-portal-runtime`, one super-admin portal on `lumi-super-admin-runtime`, and the isolated validator on `lumi-audio-validator`. All 17 Scheduler jobs use `lumi-functions-runtime`; the post-deploy `infra/iam/audit-scheduler.sh` check passes. Ordinary future logs and all three active Secret Manager payloads are Sydney-pinned, while Firebase Authentication and required audit logs remain documented cross-border exceptions. Do not recreate the deleted unsuffixed secrets; live email Functions use `SENDGRID_API_KEY_AU`/`SENDGRID_SENDER_EMAIL_AU` and the super-admin uses `ADMIN_SESSION_SECRET_AU`. Do not rebuild account/student deletion, offline-revocation, audio validation, pagination, dashboard summaries or the first-opt-in audio gate. Continue with private vendor account-acceptance evidence, APP 8/privacy-counsel approval and the remaining physical/store P2-5 checks. Support and both security-alert recipients are verified. Nicholas Plevritis holds the operational incident roles with Kylie Plevritis as authorised backup; Privacy/Legal Lead is intentionally unappointed for now.

### 2026-07-17 — shared demo-school interaction hardening (deployed and canary-verified)

- **Boundary:** The shared administrator keeps `demoReadOnly` and `demoAdminMfaExempt` so portal MFA/read-only coupling is unchanged, and receives no mobile-write capability. School administrators are routed by Flutter to the web-portal hand-off screen rather than operational app screens. Only ordinary exact demo teacher/parent claims may continue through normal role/ownership/schema checks for school-local content when the token targets the immutable demo tenant, the school is authoritatively `isDemo:true`, and `demoAccess/reseedStatus` is `succeeded`. Incomplete/cross-school/privileged shapes and running/failed reseeds fail closed. Claims are rebuilt from exact allowlists rather than inherited.
- **External effects denied:** Every demo account is blocked from callables and global mutations, including onboarding email queues, membership/role changes, SMS/MFA operations, deletion, global community catalogue/cover writes and comprehension-audio uploads. Push/email triggers also skip authoritative demo schools. Demo ISBN additions materialise only in the school-local library; shared demo audio remains playback-only rather than fabricating school authority evidence.
- **Fenced refresh:** A pure deterministic plan and ADC-only reseed command now rebuild current-week data, valid student access, real by-title allocations and school-local books. The destructive operation refuses a missing/non-demo target, collision-checks deterministic Auth users, never rotates existing passwords, preserves `demoAccess/state`, fences every phase with a lease/heartbeat, cleans only authoritative demo-owned external data, finalises exact claims only after seeding, revokes refresh tokens and atomically records success plus its audit event. Same-day password reuse does not wipe activity; first provision of a new Sydney day refreshes before credentials are issued.
- **Admin controls:** The super-admin demo page has strict same-origin, schema-validated, durably rate-limited settings/reseed routes, exact-target checks and atomic setting audits. Live controls cover messaging, parent comments/free text, quick logging and bounded comment chips; reseed progress is polled in both the school controls and onboarding panel.
- **Local verification:** Firestore Rules **156/156**, Storage Rules **16/16**, Functions **145/145**, read-only guard **6/6**, deterministic plan **2/2**, fenced Auth/Firestore/Storage reseed **2/2**, focused cover scanner **10/10**, touched-file Flutter analysis, both portal production builds, `git diff --check` and the scoped redacted Gitleaks scan pass.
- **Ordered production cutover:** Nic explicitly confirmed the production cutover. Firestore ruleset `ac8f5436-47d9-4253-9fb7-57ff9306d6c1` and Storage ruleset `f3a47e49-5bda-4b73-88a4-aab3448421b8` were deployed before interactive claims. All 75 Functions deployed with zero errors; all 77 services remained active and the 17-job dedicated-runtime Scheduler audit passed. PR #429 merged as `c5b7cd3e9a358058830b13683315c2d2fa605d22`; main-branch secret scan, admin typecheck/lint/build, identity audit and Hosting deploy passed. The active admin revision is `ssrlumidevadminau-00049-vwl` on `lumi-super-admin-runtime`.
- **Live reseed and canary:** Production reseed lease `846aaa4a-e647-4733-9f84-91ced6f82399` completed with 513 documents, 16 active students, 459 logs, three school-local books, the intended by-title/free-choice allocations, preserved daily-password state and zero global-community cleanup. Exact claims matched their then-current allowlists. Same-day provision reused the credential and retained all 459 logs. The initial authenticated Rules canary allowed the now-retired shared-admin school-local book create/delete path while denying global-catalogue and parent-onboarding-email writes; its synthetic records were removed. Demo side-effect queues were empty and a runtime-only Cloud Run/Functions query found zero error entries. On 18 July, the shared demo administrator logged into the production school portal without TOTP and a live Profile `PATCH` was rejected with `The demo administrator is read-only`; the portal boundary therefore passed its physical check. The first 18 July provision also triggered and completed a fresh production reseed (`736e3b9f-3010-4a59-946b-cce5cc7afc5e`) in about 13 seconds before issuing that day's credential: 513 documents, zero global cleanup, 16 students and the expected 459-log baseline. The mobile administrator hand-off is now physically complete; only the production reseed double-click control remains open in this demo batch.
- **18 July role correction (deny-first cutover and physical app check complete):** Nic clarified that a `schoolAdmin` must never exercise teacher-style Flutter workflows; the previous physical-test instruction and `demoInteractive` administrator exception were based on the wrong product assumption. The claim is removed from reseed/provision, revised Rules deny school-local writes even when a stale signed administrator token still contains it, and ordinary demo teacher writes remain available. The Flutter hand-off link is corrected from the retired non-AU site to `https://lumi-school-admin-au.web.app/login`. Firestore Rules pass **157/157**, the fenced Auth/Firestore/Storage reseed passes **2/2**, focused analysis is clean and the admin production build succeeds. Deny-first ruleset `70501da7-2e29-4abd-82d1-cec797c565cf` is live; the shared administrator's retired claim was stripped and its refresh tokens revoked. A fresh-token live canary returned **403** for the administrator's school-local book write and **200** for the demo teacher's equivalent authorised write, then removed both synthetic records. PR #431 merged as `88016d26813238623bfde5a0e6878b3b88f8757d`; required CI and the main admin deployment passed. Revision `ssrlumidevadminau-00051-hpc` is ready on the dedicated runtime, live root/login return 307/200, and the account still has the exact five-key claim allowlist without `demoInteractive`.
- **18 July mobile boundary recovery and physical evidence:** The first corrected-device login authenticated successfully but then hit two client writes that contradicted the portal-only boundary: `lastLoginAt` setup and the global mobile Terms gate. The login flow now routes `schoolAdmin` before teacher/parent device setup, the router sends it to `/auth/admin-portal` before mobile Terms enforcement, and any failed post-auth setup signs out rather than leaving a hidden authenticated session. The hand-off's return action now performs a real Firebase sign-out and uses a contrast-safe ink label. A properly configured App-Check-enabled iOS release build physically proved: no operational mobile or Terms screen, AU portal hand-off visible, external portal opened successfully, and sign-out returned to Login. Focused routing/login/notice tests pass **9/9** and changed-file analysis is clean.
- **18 July credential exposure response:** A physical-test screenshot accidentally included the current shared demo password. Lumi immediately replaced the passwords for all five demo Auth users with throwaway random values, revoked every refresh token, removed the plaintext daily password from `demoAccess/state`, marked that state scrambled and recorded `demo.credentialsEmergencyScramble` in `adminAuditLog`. A subsequent normal provision issued a fresh credential; the disclosed value is no longer usable and is not retained in this checklist.
- **18 July ordinary demo login regression repaired (physical retest pending):** Live ADC inspection proved the newly provisioned parent Auth user, exact ordinary-demo claims, parent profile, user-school index, successful reseed state and authoritative demo-school marker were all healthy. Authentication then failed during the permitted `lastLoginAt`/Terms self-update because those two profile paths still used the blanket demo-write denial rather than the fenced school-content helper. The Rules change is deliberately narrow: an exact ordinary demo teacher/parent may update only its own existing whitelisted profile fields after a successful reseed, while the demo administrator, privileged/cross-school claim shapes, sensitive role/link fields and unfinished reseeds remain denied. Firestore Rules pass **159/159**. Production ruleset `46ca3569-892d-4214-ac86-30ebb0f24974` is live; no reprovision or password rotation is required. Record the same-device parent login result here after retest.
- **19 July demo fail-safe gate added (final parent visual smoke deferred):** The parent retry reached the Terms route but briefly rendered its profile-load recovery state because the router could resolve `FirebaseAuth.currentUser` before the Riverpod auth stream emitted its first value. `userProvider` now uses the verified synchronous UID only while that stream is loading or transiently failed; an explicit signed-out emission remains authoritative and Firestore Rules still enforce every profile read. The recovery surface now uses current Lumi artwork/card/tokens, offers a non-destructive **Try again** before sign-out, and has widget coverage. A single local gate now covers deterministic plan **2/2**, fenced Auth/Firestore/Storage reseed **2/2**, callable read-only guard **6/6**, Firestore Rules **159/159**, Storage Rules **16/16**, focused Flutter demo boundaries **22/22**, portal security **4/4**, both portal typechecks, analysis and patch integrity. A redacted ADC live preflight verifies the current Sydney-day state, all three fresh password sign-ins, exact claims/indexes/profiles/content, real client Rules reads and the read-only portal; `--canary` also exercises and restores mobile login/Terms fields. On the first 19 July run it correctly stopped because the new Sydney day's credential had not yet been provisioned. The iPhone was disconnected before the rebuilt parent screen could be visually confirmed, so that 30-second visual smoke remains explicit rather than being claimed as passed.
- **19 July one-click super-admin readiness workflow implemented and deployed:** The terminal preflight core is now reusable by a privileged server-only route, and the onboarding demo panel exposes **Prepare and verify today's demo**. The single action provisions/reuses the current Sydney credential, performs the real redacted admin/teacher/parent sign-ins, exact-claim/profile/index/content/Rules/portal checks, and runs reversible parent/teacher login + Terms canaries while confirming the shared admin write remains denied. The API revalidates the super-admin session, requires same-origin POST plus strict confirmation input, applies durable per-actor/global limits, refuses non-demo onboarding requests and the wrong Firebase project, returns no credential/token/UID/child/document identifiers, and atomically stores a sanitised readiness receipt plus audit event. Any reseed invalidates the prior green receipt before destructive work starts. Both the UI and email API refuse to send demo credentials until today's live check is green, and credential-bearing API responses explicitly disable caching. Core success/failure redaction, reversible canary and Terms-version-sync regressions pass **4/4**; super-admin typecheck and lint pass with zero errors (six unrelated existing warnings). PRs #435 and #436 are merged, corrected production deployment `29664350523` passed, live login returns **200**, the unauthenticated readiness route correctly returns **401** with `cache-control: no-store`, revision `ssrlumidevadminau-00057-l2t` is ready and its first 30 minutes contain no error logs. The sole remaining evidence is a signed-in super-admin click producing today's green readiness receipt; no controllable signed-in browser was available to automate that privileged step.
- **19 July first privileged readiness run diagnosed and repaired:** The signed-in run passed tenant/reseed, credential, content, all three identities, claims, profiles, indexes and production Rules, then reported the portal stage as failed even though the school portal had correctly rejected the demo administrator mutation with **403 Forbidden**. The verifier had incorrectly accepted only the route-level **401** defence and not the earlier middleware **403** defence. It now accepts only those two denial statuses and still fails if the request reaches validation (**400**) or succeeds. Regression coverage is **5/5**, and the full demo readiness/security gate passes. Because the screenshot included the active shared password, the production scramble job was run immediately: all **5/5** scoped demo accounts received unstored random passwords, refresh tokens were revoked, the day state was marked scrambled and there were zero failures. The portal now masks a newly provisioned password by default and requires an explicit reveal action, while retaining copy support.

### 2026-07-19 — live shared-demo controls and visual seed hardening (production cutover complete)

- [x] **Operator clarity:** The onboarding Demo access action now changes from **Prepare and verify today's demo** to **Reprovision and check today's demo** once a non-scrambled Sydney-day password actually exists. An incomplete state without a password is no longer treated as active.
- [x] **Colourful deterministic seed:** All 16 synthetic students receive different selectable Lumi character IDs. The deterministic plan test proves the IDs are unique, match every seeded child and resolve to bundled assets; the legacy manual seed has the same character mapping.
- [x] **Live controls:** The Demo access card and permanent demo-school controls tab share one real control surface for audio preview, parent comment chips, typed comments, parent–teacher communication, quick logging and bounded chip/category editing. Defaults remain populated after every fenced reseed.
- [x] **Privileged boundary:** Both API entry points independently require a current super-admin session, same-origin mutation, strict Zod allowlists and Firestore-backed actor/global rate limits. The service pins the exact production project, immutable configured demo tenant, authoritative `isDemo:true` marker, active daily credential and (for onboarding) a real demo request. Audit records contain booleans/counts only, never custom chip text.
- [x] **No fabricated voice authority:** Enabling demo audio stores only `enabled` plus `demoPreviewOnly`; it does not create authority/consent/retention evidence. The parent app labels the local preview, supports capture/playback, discards the private file on completion, and never enters Storage or confirmation-callable upload paths. Existing server and Storage denial for every shared demo account remains unchanged.
- [x] **Verification:** Focused control/model tests **7/7**, plan tests **2/2**, audio-preview Flutter tests **4/4**, changed-file Flutter analysis, admin lint/typecheck/production build and the complete demo regression gate pass. The full gate still includes fenced reseed **2/2**, callable guard **6/6**, Firestore Rules **159/159**, Storage Rules **16/16**, mobile login/route/Terms recovery, and school-portal session/audio-authority checks.
- [x] **Production cutover:** PR #439 merged as `30d2be109a3d5a4fde0b56dde883291780b9f61b`. Required admin CI, complete demo regression and history secret-scan checks passed. Main deployment run `29667410450` completed in 9m46s after re-verifying the dedicated build/runtime IAM identities, deploying Firebase Hosting and disabling the redundant framework auth bridge. Live login returns **200** and the unauthenticated control API fails closed with **401**.
- [x] **Live reseed and canary:** Controlled production lease `8c411611-c3d5-4517-97f1-9f41eee8f0aa` completed with **513** documents and zero global-community cleanup. Aggregate Firestore verification found **16/16** students with assigned characters and **16/16 unique** IDs. The seed restored audio off/preview-only, three populated comment categories, typed comments, messaging and quick logging. The redacted live canary passed all six tenant/content/credential/admin/teacher/parent/Rules/portal/reversible-write checks.
- [x] **Explicit reprovision repaired and production-proven:** Production audit evidence showed that the first three apparent reprovision clicks were still ordinary same-day provisions (`reused:true`), so they correctly skipped both credential rotation and reseed; the reseed also did not advance the separate control-status timestamp used to remount an already-open control panel. The active UI now sends a strict `reprovision` action, the server refuses non-demo onboarding targets, applies tighter **3/actor and 6/global per hour** limits, forces the fenced reseed, explicitly rotates the daily credential, and advances the default control summary in the same transaction that completes the reseed. Ordinary first preparation remains idempotent. Focused controls pass **10/10**, fenced reseed/password tests **3/3**, and the complete demo readiness/security gate plus production build pass. PR #441 merged as `2532f64`; deployment run `29668646229` completed in 9m46s with the dedicated IAM checks and auth-bridge repair. The signed-in production retest recorded `mode:reprovision`, `reused:false`, a later credential issue time, a successful **513-document** reseed with zero global cleanup, audio off/preview-only, comments/free text/messaging/quick logging on, **3** default categories and **12** chips. Readiness was green with **6/6** checks, the independent redacted reversible canary passed all six stages, and the seed still had **16/16 unique** Lumi characters. No password or child identifier was included in the verification output.
- [x] **Physical local-only demo-audio evidence complete:** A fresh App-Check-enabled Android debug build on the Samsung A57 used only fictional demo child Ava and displayed the explicit **“not uploaded or kept”** notice. The operator captured a three-second synthetic recording, completed the preview/playback path and selected **Use this recording**. While the preview remained open, device inspection proved exactly one `.m4a` existed inside Lumi's private debuggable-app storage and both canonical and staging demo-school Storage prefixes remained empty. Saving created the expected reading log (**459 → 460**) while the private file changed **1 → 0**; an aggregate scan found **0/460** logs with an audio path, duration or uploaded flag and Storage remained **0/0**. The live control was restored to off and authoritative Firestore verification confirmed `enabled:false` while retaining `demoPreviewOnly:true` for the next explicit opt-in. No voice payload, password, child identifier or document ID was read or printed during verification.

### 2026-07-19 — temporary demo book-allocation workflow (implementation in progress)

- [x] **Failure diagnosed before changing permissions:** The Samsung camera assignment failure was not a Rules denial. Device logs at the matching time showed Firestore `UNAVAILABLE` plus `UnknownHostException` for `firestore.googleapis.com`. The repair therefore preserves the authorization boundary and handles only genuinely transient transaction failures.
- [x] **Flutter camera/manual implementation:** ISBN transaction failures classified as `unavailable`, `deadline-exceeded`, `aborted`, DNS/socket or timeout are queued idempotently; `permission-denied`, validation and App Check failures are never reported as saved. A permission denial gets one bounded token refresh/retry. Camera and manual demo allocations carry `demoEphemeral:true`, the current `demoGenerationId` and an allowlisted origin. Camera allocations use a demo-only deterministic document ID so they cannot collide with seeded records. Queue payloads retain the generation; a missing generation in a verified demo or a concrete old/current mismatch is parked after reprovision, while a temporary inability to verify the current claim stays retryable. UI diagnostics expose only bounded error codes, not raw backend payloads.
- [x] **Restricted school-portal implementation:** The ordinary demo portal remains read-only. Only three dedicated routes permit demo allocation create, ISBN assign and whole-allocation delete. Each route independently requires the exact signed demo-admin capability, same-origin browser request, current active membership, authoritative `isDemo:true` tenant, successful matching reseed lease, strict bounded Zod input, Firestore-backed per-actor/tenant rate limits and minimal hashed audit records. Portal ISBN lookup does not create school/community catalogue records. Seeded allocations are visibly read-only; only current temporary allocations can be deleted. The create UI is restricted to weekly by-title allocations.
- [x] **Generation fencing wired through reprovision/provision:** The reseed lease is added to the exact custom-claim allowlist for all shared accounts, same-day provisioning reapplies that exact current claim without rotating the password, the portal session copies it only after server-side tenant/reseed verification, and every temporary mutation compares it with the live reseed status. A reprovision therefore invalidates old cookies, tokens and queued allocation work.
- [x] **Rules and automated verification:** The Android Studio bundled JDK was used to complete the previously blocked emulator run. The full Firestore Rules suite passes **161/161**, including current-generation temporary create/update/delete, unmarked create denial, seeded update/delete denial and stale-generation denial. The expanded assignment/offline suite passes **58/58**. The complete demo readiness regression gate passes: Flutter changed-boundary tests and analysis, both portal typechecks, portal security tests **6/6**, super-admin demo preflight **5/5**, controls **10/10**, deterministic plan **2/2**, fenced reseed integration **3/3**, Functions guard **6/6**, Storage Rules and patch integrity. Both the school portal and super-admin portal production builds compile successfully.
- [x] **Ordered backend deploy and live canary:** PR #444 merged as `c41bc16`. The school portal deployed first as revision `ssrlumischooladminau-00074-ziv`; the super-admin deployment run `29671777189` then passed in full as revision `ssrlumidevadminau-00065-9rk`, with each portal retaining its dedicated runtime service account. A controlled reseed of only `lumi_demo_primary_school` wrote **513** documents, deleted **0** community books, minted current generation claims and preserved today's existing password. The redacted six-stage production preflight passed both before and after the Firestore Rules release. A final aggregate-only check found exactly **2 seeded / 0 temporary** allocations, so the seed remained unchanged. No password, token, UID, child name or document ID was captured in the evidence.
- [ ] **Mobile distribution:** The generation-fenced Flutter implementation is merged, but no App Store/Play build has been distributed while Lumi's Apple/Google business enrolment is pending. Use a fresh local physical-device build for the acceptance matrix below; do not represent the mobile rollout as production-complete until that matrix and the eventual store build pass.
- [ ] **Physical acceptance:** On a fresh provisioned teacher login, test camera assignment while online, forced offline/DNS fallback and later single replay, manual allocation create/delete, seeded allocation delete denial, parent visibility, and reprovision removal of every temporary allocation. On the portal admin, test weekly by-title create, ISBN assign, temporary delete, seeded delete disabled/denied and stale-cookie 409 after reprovision. Record document counts only; do not capture passwords or child/document identifiers.

### 2026-07-17 — error paths, cross-parent matrix, residency and operations

- **Client failure paths hardened:** An authoritative own-profile permission denial, disabled/missing user, expired or revoked token now invalidates the full local session. Transient Firebase/network failure stays on a bounded retry screen and cannot be mistaken for a missing profile or create a login-route loop. Catalog outages no longer strand the book flow: community and public lookup failures fall through to labelled manual entry. Existing empty-class/library and camera-denial/unavailable/timeout fallbacks were retained. Focused tests pass **96/96** and changed-file analysis reports no issues.
- **Consolidated family boundary passed:** A single Firestore matrix now proves Parent A can access its own membership/child/log/comment and scoped history while being denied Parent B membership, child, log, comment, targeted/unfiltered queries, update/delete and a forged comment. Storage uses a legitimate unrelated Parent B account and denies a forged owner-metadata upload into Parent A's audio path. Firestore Rules pass **151/151** and Storage Rules **13/13**.
- **Australian resource audit completed:** Firestore, child-content Storage, all 77 live application services, future ordinary logs and all three active application-secret payloads are in Sydney. The former automatically replicated secrets were cut over and deleted on 17 July. Firebase Authentication is officially US-only; FCM/App Check/Analytics/Crashlytics and required audit logs remain global, and SendGrid remains cross-border. A legacy unreferenced US Functions source bucket contains historical code archives rather than identified child records and must not be deleted blindly. Full evidence: `docs/security/AU_RESOURCE_LOCATION_AUDIT_2026-07-17.md`.
- **Vendor/APP 8 evidence prepared:** Official current OAIC APP 8 Guidelines v1.3, Google Cloud DPA/subprocessors/service terms, Firebase processing/standard terms and Twilio DPA/subprocessors/security/SendGrid-retention pages are retained under `docs/privacy/vendor-evidence/2026-07-17/` with source URLs and SHA-256 hashes. `docs/privacy/APP_8_CROSS_BORDER_DISCLOSURE_LAWYER_BRIEF.md` explains the law, Lumi flows and counsel decisions. This closes the public-document collection step, not account-specific acceptance or legal approval.
- **Regional-secret migration completed:** New user-managed `ADMIN_SESSION_SECRET_AU`, `SENDGRID_API_KEY_AU` and `SENDGRID_SENDER_EMAIL_AU` resources each have one `australia-southeast1` replica and only their dedicated runtime accessor. Payload equality was verified without output, and SendGrid accepted the migrated key with `mail.send`. Functions **141/141** and both production builds passed. Five email Functions and `ssrlumidevadminau` deployed successfully; all six were `ACTIVE` on their dedicated identities with only the new secret names, live admin login returned HTTP 200, root redirected to login, and no post-cutover error log was found. No current Function referenced an old name, then the three globally replicated predecessor resources were deleted and absence verified.
- **Authenticated admin SSR regression repaired:** The static login probe did not exercise Firebase Hosting's preview authenticated-server-context bridge. With an existing `__session` cookie, that redundant bridge called Identity Toolkit server-side using the correctly referrer-restricted browser key and returned HTTP 500 because a server request has no browser referrer. Lumi already verifies the HttpOnly session independently with the Admin SDK, so the bridge is now forced off after each deploy and the Hosting-pinned tag is moved to the repaired revision. The browser key remains restricted. Safari authenticated successfully after the live repair; the workflow now performs the same repair on every admin deployment. The deploy identity has metadata-viewer access to `ADMIN_SESSION_SECRET_AU` solely for Firebase version validation and cannot read its payload. A failed least-privilege CI deploy also exposed that Cloud Functions still used the default Compute account for the **build** even though the live runtime was dedicated. The portal build was migrated to keyless `lumi-admin-build`, scoped only to logs plus the Sydney source bucket and build-image repository; it has no child-data or runtime-secret access. A pre-deploy audit now fails closed on build/runtime identity, permission or key drift. GitHub receives only a custom read-only auditor role for the required policy/key metadata; it cannot read secret payloads or modify IAM. The admin workflow and generated function runtime are upgraded from deprecated Node 20 to Node 24. A production-equivalent Node 24 dry run and the final main-branch deployment both passed. Post-deploy production evidence confirms Node 24, the dedicated build/runtime identities, live login/root HTTP 200/307, an authenticated disposable session/dashboard/Storage signing/reconciliation canary and zero recent Cloud Run errors; the canary removed all synthetic resources in `finally`.
- **Incident governance recorded:** Nicholas Plevritis, Director of Lumi, holds incident lead, technical coordination, school liaison, communications and scribe responsibilities. Kylie Plevritis is authorised to act for Nicholas and Lumi on urgent operational containment/provider/school communications if Nicholas is unavailable. The Privacy/Legal Lead remains deliberately unappointed; Kylie is not represented as legal counsel and must obtain external legal advice for legal determinations.
- **Live Scheduler drift repaired:** The audit found three jobs returning HTTP 403 because older Scheduler definitions still minted OIDC as the App Engine default account while the corresponding services correctly allowed only the dedicated runtime. All 17 jobs were migrated to `lumi-functions-runtime`; the deletion and notification five-minute jobs then returned success and wrote current `ok` heartbeats. The final obsolete default-account service invoker was removed. The checked-in audit fails on recurrence; see `infra/iam/audit-scheduler.sh` and `docs/security/OPERATIONS_HEALTH_AUDIT_2026-07-17.md`.
- **Production log payloads minimised:** Routine Functions logs no longer include account, school, child, reading-log, document/object-path or email identifiers, and raw exception messages/objects are replaced by bounded machine error codes. Detailed workflow failures remain only in protected Firestore job/audit records. A source-wide regression scans every TypeScript logger call for prohibited identifier interpolation/fields and raw error payloads. Functions pass **139/139**; 75 Functions were updated with zero deployment errors, all 77 services remained active/ready, the Scheduler audit still passed, new five-minute heartbeats were `ok`, the Sydney bucket received post-deploy traffic, and no post-deploy error or prohibited-identifier payload was found.

### 2026-07-17 — iPhone diagnostics and offline-revocation evidence

- **Direct-installed release-mode privacy capture passed:** An Apple Development-signed, App-Check-enabled iOS release build was cold-started on a physical iPhone. Before opt-in, traffic included the required Firestore/App Check/development App Attest endpoints but no Firebase Analytics `app-measurement`, Firebase Installations, Crashlytics settings/logging or crash-upload endpoint. After the adult enabled both **Settings → Account → Privacy & diagnostics** controls and restarted, Analytics/Installations traffic appeared. After both controls were withdrawn and the app restarted, those endpoints disappeared again. No crash was deliberately generated, so this proves Crashlytics' fail-closed startup state but not an opted-in crash payload. It is direct-install evidence, not App Store App Attest or live questionnaire evidence. The three raw packet captures were deleted immediately after endpoint extraction and were never committed.
- **Physical offline revocation passed:** Using fictional demo child Leo only, the parent saved a synthetic reading log with Airplane Mode and Wi-Fi off. Lumi confirmed it was saved locally and showed one pending reading log. Leo's server-owned complimentary access was then temporarily suspended while the phone remained offline. On reconnect, Firestore denied the write and Lumi retained it visibly as “couldn't sync”. A server-side count remained exactly **50**, proving the rejected create did not partially land or bypass access rules. Leo's original active complimentary access and expiry were restored transactionally after the denial evidence was captured.
- **Recovery UX defect found and repaired:** The installed build showed the raw Firebase `permission-denied` text and its global **Try syncing now** action skipped parked items. User-facing surfaces now map stored backend errors to safe, actionable wording without exposing implementation details. An explicit retry makes one fresh attempt at parked items only after the write-path probe is healthy; background drains still skip them, an offline retry leaves them parked, and a failed retry remains parked rather than being discarded or hammered. Focused offline tests pass **39/39** and changed-file analysis reports no issues. Final recovery of the preserved on-device item remains the immediate resume step after installing this patch; it is not needed to prove the access-revocation denial itself.
- **Second recovery defect identified and repaired:** The first explicit recovery attempt left the row queued but hid the top pill because a recent transient failure on an otherwise healthy connection was neither parked nor 48 hours old. Server verification remained at 50, so the disappearing pill was correctly not accepted as success. A narrow read-only copy of the synthetic pending Hive box showed the intact Leo item failing local replay at retry 6 because Hive restores an empty/nested map as `Map<dynamic, dynamic>` while `ReadingLogModel.fromLocal` cast it directly to `Map<String, dynamic>`. Local decoding now safely coerces metadata and comment-view maps, ignores malformed entries, and has exact dynamic-map regressions. Every non-empty queue now stays visibly surfaced until server confirmation, using an amber waiting state for normal retries and red only for parked/stale items. The temporary copied Hive diagnostic was deleted immediately. The combined focused model/provider/offline run passes **68/68** with clean changed-file analysis.
- **Authoritative recovery passed:** The corrected release build preserved the same random log ID and the scheduled retry succeeded after access restoration. The Leo server count moved from **50 to 51**; the exact queued ID arrived with five minutes, a server receipt at 08:07 AEST and `isOfflineCreated: true`. A post-install relaunch confirmed the ID exists exactly once, so recovery did not duplicate the event. The UI retained the amber pending pill until confirmation. A final usability defect was isolated: **Try syncing now** respected an existing transient backoff and reported `0 synced` instead of making an immediate attempt. Explicit retries now clear both parked state and transient backoff only after the write-path probe is healthy; automatic drains still retain bounded backoff. The added regression brings the focused offline suite to **40/40** and the combined model/provider/offline run to **69/69**. PRs **#413**, **#414** and **#415** passed the required secret scan and were squash-merged; the final build is installed on the iPhone and all temporary capture/diagnostic files plus `rvi0` were removed.

### 2026-07-17 — production monitoring and bounded membership resolution

- **P2-1 technical rollout live:** Five metric policies cover Firestore reads (>20,000/hour), Firestore writes (>5,000/hour), Cloud Run egress (>50 MiB/hour), Firebase user-content Storage egress (>50 MiB/hour) and the user-content Storage footprint (>250 MiB). Three log-match policies cover comprehension-audio security failures, deletion job failures and developer-impersonation anomalies. Thresholds sit above the measured 30-day hourly peaks (9,689 reads, 1,964 writes, ~4.5 MB Cloud Run egress and ~1.5 MB Storage egress); both Storage policies are explicitly scoped to `lumi-ninc-au.firebasestorage.app`, excluding build/source buckets. Together with the five existing function/security/auth policies, production has **13 enabled alerts** attached to both security email channels. Firestore DATA_READ and DATA_WRITE audit logging is enabled through the project IAM audit config. The checked-in idempotent source and apply script are under `infra/monitoring`.
- **Security/cost dashboard live:** Cloud Monitoring dashboard `Lumi production security and cost` graphs Firestore read/write volume, Cloud Run 5xx and egress, Firebase user-content Storage egress/footprint, App Check denials and Firestore Rules errors. Re-running `infra/monitoring/apply.sh` updated all five policies and the dashboard without duplicates.
- **Delivery incident closed:** A temporary isolated custom metric opened alert `0.oabm7h2yj055` at 23:43:34 UTC and targeted both named channels. The deliberately firing policy was disabled immediately after the incident was observed. Nicholas Plevritis confirmed receipt in both inboxes on 17 July 2026, closing P2-1; no production endpoint or child data was used.
- **Unbounded login fallback removed:** `resolveUserSchoolByUid` now reads the server-owned `userMembershipIndex/{uid}`, verifies the authoritative membership, and uses only a bounded top-level legacy-index query during migration. Two least-privilege Eventarc triggers maintain the UID index on parent/staff writes; clients are denied all reads and writes. A one-time production backfill found 38 unique memberships, zero conflicts and converged to zero remaining writes. Functions tests pass **132/132** and Firestore Rules pass **149/149**. A disposable production canary proved trigger create → callable resolution → trigger delete, Auth/Firestore and FCM. The first canary correctly exposed missing Run Invoker on the two new services; only service-level bindings were added and project-wide Run Invoker remains absent.
- **P2-3/P2-6 operating controls drafted and exercised:** `docs/privacy` contains a working PIA, vendor/data-flow register, breach-response plan and 17 July cross-school exposure tabletop. A release privacy/security gate and recurring review schedule are checked in, with a weekly/manual GitHub Actions workflow for Functions, Rules and critical/high production dependency audits. This closes P2-6's scheduling requirement. Nicholas Plevritis and backup Kylie Plevritis now hold the operational incident roles. P2-3 remains partial until owner/counsel approve the PIA/APP 8 decisions, privacy/legal leadership is appointed and school emergency contacts are recorded.
- **P2-4 measured:** A real-Rules emulator profile passed at 30, 100 and 1,000 students. Weekly raw-log reads were 210/700/7,000; recent activity stayed at 15; a 12-week calendar projects 2,520/8,400/84,000 documents. Manual listener ownership/disposal paths were audited with no orphan found. The foreground server probe moved from 180s to 600s, cutting its periodic steady-state reads by 70%, while event-driven health checks remain. All-time parent/teacher histories and the optional raw-log calendar remain open; see `docs/security/COST_SCALE_AUDIT_2026-07-17.md`.
- **P2-4 closed after migration:** Parent All-time/Bookshelf and teacher student history now use stable 30-record snapshot-cursor pages. Calendar, weekly chart, teacher intelligence and home-widget windows now use server-written `classDailyReading` shards; summaries expose no titles, notes, comments or audio metadata. Duplicate/out-of-order log events converge transactionally, Eventarc retries transient failures, and a weekly Melbourne reconciliation repairs drift. Rules deny every client write and restrict teacher reads to assigned classes; the composite index is READY. Production backfill/verification matched **697 logs and 12,857 minutes** exactly across **459 shards**, with zero inconsistent shards. The post-migration real-Rules profile held summary reads at 56/week and at most 672/12 weeks for 30, 100 and 1,000 students; only an optional detailed current-week path retains bounded raw logs.
- **P2-5 force-update fail-safe implemented:** The audit found startup attempted to read `RemoteMessageController.instance` before creating it, caught the exception and left the UI-created controller inert. Bootstrap now creates and initializes the singleton before mounting the app. A first successful response or cached policy is explicitly `available`; an unreachable first fetch, missing release endpoint, malformed minimum version or unreadable installed version enters a retry/support screen instead of silently allowing an incompatible client. A later outage preserves the cached policy. Supported mobile builds now require an HTTPS status-worker setting. A deliberately misconfigured ad-hoc iOS release physically entered the blocking support state, while a rebuild using `.dart_define.json` reached the valid app flow. The checking/support/update pages now share the current Lumi card/tokens/typography, use `assets/UI Lumi/lumi welcome.png`, and retain the policy decision outside the presentation widget so the redesign cannot weaken blocking. The live worker returned a structurally valid empty policy; evaluator/UI tests pass **19/19** in the latest focused run and changed-file analysis is clean. Android derives its Play URL from the package ID; the iOS store URL cannot be filled until App Store Connect creates the listing.
- **Portal dependency remediation deployed:** The root workspace now includes both portals in one frozen lockfile and one recurring production audit. Both portals use patched, Firebase-compatible Next.js **15.5.20**; Firebase client packages were aligned, and patched transitive versions are pinned for the disclosed gRPC, multipart, Hono, protobuf and WebSocket chains. Firebase Hosting currently requires Firebase Admin v13, so all shared portal packages use supported **13.10.0** rather than undeployable v14. A Next 16.2 attempt was rejected after its Turbopack artifact omitted a hashed Admin external; the broken revision was replaced immediately. Both final Cloud Run revisions are ready on their dedicated identities, public routes return HTTP 200, both live capability canaries pass and the combined production audit is 0 critical/high.
- **Physical student and account deletion passed:** The first iPhone student request correctly exercised confirmation gating but exposed a missing production `notifications.studentIds` collection-group array index and showed raw `INTERNAL` in the client. The idempotent job retained its recoverable state. After deploying the missing index with all inherited single-field modes and live TTL overrides preserved, the scheduler completed attempt 2. Production verification proved the student, reading log, both audio objects and link code absent; class/group/allocation/campaign references removed; the demo-school student counter restored from 17 to 16; and the completed 90-day receipt stripped raw requester/student identifiers. The account screen now maps internal failures to actionable non-sensitive text. Account-deletion preflight subsequently found and repaired missing collection-group ascending indexes for `comments.authorId` and `deletionRequests.requestedBy`; both reached `READY` and their exact production queries passed. The physical account request then completed on attempt 1 and returned the app to Login. Auth, staff/top-level user, membership/index, feedback and notification records are absent; school-authored fixture records were deidentified; teacher/student counters returned to 2/16; and the minimal receipt contains no raw requester UID. Four intentionally preserved synthetic school-owned records were deleted after verification, leaving only the 90-day receipts. Functions pass **144/144**, including index/TTL regressions.
- **Inline recent-login recovery implemented:** The physical account test showed that the secure recent-auth gate worked but forced a disruptive sign-out/sign-in round trip. Parent and teacher Account screens now catch only the server's explicit `recent-login-required` response, ask the current Firebase user to reauthenticate, force-refresh the same UID's token and retry the already-confirmed deletion once. Password accounts show a read-only email plus obscured password field; phone-primary and password+SMS-MFA accounts use the existing rate-limited SMS path; Apple/Google provider routes are supported for future linked accounts; unsupported providers fail closed to a full login. Cancelling or failed verification never retries, credentials never enter the callable payload or logs, and the server `auth_time` check remains authoritative. Moving the `DELETE` and password controllers into route-owned widgets also fixed a real dispose-during-dialog-transition fault exposed by the new immediate modal. Focused deletion/reauthentication tests pass **19/19**, changed-file analysis is clean, full analysis has no errors/warnings (122 existing info notices), and the unsigned iOS release build succeeds.

### 2026-07-16 — store-account wait: monitoring and unknown-key attribution

- **Store-only blockers recorded:** Organisation enrolment is waiting on a DUNS number, so Play App Signing, Play Integrity production attestation, Apple distribution/TestFlight, production App Attest, final signed AAB/IPA scans and live store questionnaire evidence are paused. This does not block IAM/WIF, monitoring, local App Check negative tests, offline policy/tests, privacy documents, cost/load tests or direct-install Android testing.
- **Physical Android available:** Samsung A57, Android 16, One UI 8.5. Use synthetic/demo child data only when running the pending offline, consent, audio and error-path checks. A direct USB install does not require a Play Console account; repeat the security smoke test later with the Play-signed artifact.
- **Monitoring recipients and policies created:** Enabled Cloud Monitoring email channels now exist for primary `nic@lumi-reading.com` (channel `6689107490351214266`) and backup `nicxplev@gmail.com` (channel `11207813659675718192`). Both channels are attached to five enabled policies: Cloud Run 5xx spike (>10/5m; observed seven-day max 6), Firestore rules evaluation-error spike (>50/5m; observed max 21), App Check denials (>20/5m), any Firebase Auth free daily sign-in quota exceedance, and blocked authentication SMS (>5/5m). Receiving a real test/incident message must still be confirmed before treating notification delivery as proven.
- **Unknown Admin key attributed:** Key `ade3…e687` was created at 2026-07-10 06:18:52Z by `nicplevrit7@gmail.com` from this Mac. The Cloud Audit entry identifies a scripted Claude Code `gcloud iam service-accounts keys create` command. The originating session confirms it was approved solely so demo seed scripts could create Firebase Auth users and wrote the credential to `~/.keys/lumi-ninc-au-admin.json` with mode `600`. The file still exists; repository/config/common-folder scans found no copy, and Cloud Audit Logs show no request explicitly authenticated with this key after creation. This is an attributable local tooling credential, not an unexplained external key, but it remains a long-lived Firebase Admin credential.
- **ADC replacement and key retirement complete:** Application Default Credentials are an `authorized_user` login with quota project `lumi-ninc-au`, and the active `gcloud` project is `lumi-ninc-au`. With both key-path environment variables unset, Firebase Admin successfully listed one Auth user and read `platformConfig/incrementalAggregation`. Key `ade3…e687` was then disabled; the same ADC checks still passed while an explicit attempt through the JSON failed with `app/invalid-credential`. All 69 AU Cloud Run services reported healthy and the marketing, school-admin, privacy-policy, super-admin and direct SSR login endpoints returned HTTP 200. The cloud key was permanently deleted and `~/.keys/lumi-ninc-au-admin.json` was removed; the Firebase Admin service account has no remaining user-managed keys.
- **Least-privilege IAM migration complete:** Four checked-in custom-role definitions and three new runtime accounts now express the application boundary. All 70 application Functions run as `lumi-functions-runtime`; the school and super-admin SSR backends each have their own identity; the media validator remains on its zero-data-permission identity. All four runtime accounts have zero user-managed keys. Project `Editor` was removed from both App Engine and Compute defaults; only Google's managed Cloud Services agent retains that primitive role. Firestore/Auth/FCM, bucket access, secret access, self-scoped `signBlob`/`getOpenIdToken`, validator invocation, both portal sessions/uploads, a fresh post-removal Cloud Build/deploy and a real Scheduler run all passed with disposable synthetic data. The scheduled reconcile scanned 27 objects / 2,799,757 bytes in 1.128s with zero drift. No post-fix IAM/permission errors were logged. The first super-admin canary also exposed and fixed a Firebase Hosting wrapper/browser-key conflict by moving that portal to a separate Secret-Manager-backed signed cookie with per-request Auth revocation and `/superAdmins` validation; the browser key remains referrer-restricted. Source of truth and rollback notes are in `infra/iam/README.md`.
- **Android baseline installed:** Flutter and the Android toolchain detect the USB-connected Samsung SM-A576B (Android 16/API 36). A fresh debug APK built and installed as `com.lumi.lumi_reading_tracker`; its `MainActivity` became the resumed activity and no Lumi fatal exception appeared in the captured log. This first launch intentionally used the default App Check-off build to separate basic Android compatibility from attestation behavior. The interactive synthetic-data/offline/audio/consent checks and App-Check-enabled debug canary remain.
- **Physical test found and fixed Android restore leakage:** The first uninstall/reinstall on the Samsung restored a prior authenticated Lumi session and immediately began real-school Firestore queries. Testing stopped, the app data was cleared, and the temporary App Check debug token was revoked. Although the manifest already set `allowBackup=false` and `fullBackupContent=false`, Android 12+ can ignore those controls for device-to-device migration on some manufacturers. `android:dataExtractionRules` now points to an explicit rules file that excludes every credential/device-protected root, file, database, shared-preference and external domain from both cloud backup and device transfer. The rebuilt APK packages all exclusions. A second full uninstall/install restored no Auth preference/database before launch and produced no authenticated school query. This protects session/offline child data from Android transfer; Android 16 QPR2 cross-platform transfer policy should be revisited once the Apple Team ID is available.
- **Android App Check debug canary valid:** After the clean reinstall, an App-Check-enabled debug build activated the Android debug provider. Its secret was captured without printing/committing it, registered through the Firebase App Check management API, and the temporary Mac logs were removed. Cloud Monitoring then showed the registered Android app ID as `VALID/ALLOW` for Firestore (187 sampled verifications) and Identity Toolkit/Auth (3); the earlier `UNKNOWN/INVALID` traffic remains allowed because enforcement is still off. The first token used during the interrupted restored-session run was revoked. The clean canary token was also revoked and the app data/package removed after the interactive login used a non-demo production tenant; no offline, audio, deletion or other mutation test was attempted. Repeat interactive tests only with an explicitly identified synthetic/demo credential. This proves the Flutter/App Check/Firebase path with a debug provider, not Play Integrity. Keep production enforcement off until store-signed Play traffic and legacy-client migration are healthy.
- **Demo audio canary lifecycle complete:** The test used only the synthetic parent `support+demo.parent@lumi-reading.com` in confirmed demo tenant `schools/lumi_demo_primary_school`, with two fictional children and a synthetic comprehension prompt. The App-Check-enabled Samsung canary completed capture/upload/recovery testing. After evidence collection, the audio was deleted through the production callable, the synthetic test log was removed, demo-school recording was disabled again, debug token `Samsung A57 demo audio test 2026-07-16` was revoked, app data/package were removed, and USB stay-awake was disabled. Reinstall a fresh registered canary before any later physical teacher-UI/offline test; do not reuse an old debug token.
- **Demo-student access unblocked:** Leo and Ava, the two fictional students linked to the synthetic demo parent, now have server-written complimentary access for academic year 2026 with an expiry at the end of January 2027. Their access source is recorded as `comp`; no client-editable role or entitlement field was trusted. The Samsung refreshed into Leo's three-step reading-log flow without a Lumi fatal exception, so the physical microphone/upload/playback test can proceed using synthetic content. A visible 2-pixel right-edge overflow in the feelings control was noted as a separate UI defect. Do not grant or test access in a real school tenant.
- **Android parent audio capture/upload proven, with two live defects repaired:** The Samsung granted microphone permission, recorded a synthetic four-second mono AAC/M4A answer and played it back clearly with no Flutter/Firebase/permission failure. The first save exposed a rule/client compatibility bug: the installed client serialised `loggedByRole: null`, while the parent-authorisation branch defaulted only an absent role to `parent`. Parent UID, linked child, class, school and live access were all valid, and no partial log/object existed. Rules now safely accept null as the legacy parent representation while retaining every ownership/schema/access check; new client logs explicitly write `loggedByRole: parent`. The exact null-payload regression is included in the **146/146** Firestore suite and the focused Flutter service suite passes **8/8**. Firestore ruleset `d2d8ee42-0354-458d-a2ef-a08ef50241a0` is live. The retry created the log, then exposed a production-only Storage 403: the Firebase Storage service agent lacked `roles/firebaserules.firestoreServiceAgent`, so `firestore.get()` inside Storage Rules could not evaluate the owner log. The managed role is now bound only to `service-3795320704@gcp-sa-firebasestorage.iam.gserviceaccount.com`; the temporary incorrect binding was removed. The preserved offline upload retried automatically and cleared the queue. Server verification found the canonical object and log receipt: `audio/mp4`, 38,834 bytes, four seconds, valid ISO-media `ftyp` signature, matching school/log/owner/student/duration metadata, `comprehensionAudioUploaded == true`, and a server timestamp. No real child data was used. Source and regressions were merged in PR **#405** as `c3efec7`.
- **Production teacher playback, signer and deletion proven:** A temporary production-call harness used in-memory random passwords and short-lived Auth identities only against the synthetic demo tenant. The teacher assigned to Leo's class received HTTP 200 and a 900-second signed read URL; a 32-byte range fetch returned HTTP 206 with a valid `ftyp` signature. An unassigned same-school teacher and a linked parent each received HTTP 403 `PERMISSION_DENIED`; an unauthenticated caller received HTTP 401 `UNAUTHENTICATED`. The assigned-teacher deletion callable then returned HTTP 200, removed the object, cleared the receipt fields, stamped a server deletion time and wrote the admin audit event. URLs, tokens, passwords and audio bytes were never printed or stored. All temporary Auth users and membership documents were deleted in `finally` and a post-check found zero remnants. This proves class-scoped production callable authorization, real Cloud IAM signing and audited deletion; it does not replace a physical teacher-UI or enforced-App-Check test.
- **Callable App Check physical-device canary complete and restored:** Enforcement and token consumption were temporarily enabled only for `confirmComprehensionAudioUpload`, `getComprehensionAudioUrl` and `deleteComprehensionAudio`. The synthetic demo parent recorded and saved an iPhone AAC/M4A file, and server verification found the canonical object plus a generation-bound `ffmpeg-aac-mono-v1` validation receipt, bounded duration, SHA-256 and expected media type/size. The synthetic demo teacher played it clearly and deleted it on the Samsung; Cloud Logging recorded `APP VALID` + `AUTH VALID` for playback/deletion, the audit event exists, the log audio fields are cleared and both pending/canonical objects are absent. A request without App Check returned HTTP 401 `UNAUTHENTICATED` with `app=MISSING`. The client now requests limited-use tokens for all three audio callables and the server explicitly rejects `request.app.alreadyConsumed`; focused Functions and Flutter tests cover both halves. The physical delete exposed a UI-only defect: the callable succeeded but the comments sheet left the player spinning until dismissal. The player now sets its own deleted state immediately and a widget regression proves it disappears without relying on a host rebuild. Enforcement was then redeployed off on all three callables. A PITR-backed exact restore left the platform flag at its prior `enabled=true`, demo recording off, Beaumaris recording on and exactly one active opted-in school; the synthetic log/audio and validation-rate row were removed. Both temporary debug registrations were revoked and verified 404, all local token/log/build artifacts were removed, Android app data was cleared and iOS was force-signed-out/cache-cleared. The phones disconnected before package uninstall, so remove the inert debug packages at the next USB connection. `roles/firebaseappcheck.tokenVerifier` intentionally remains only on the Functions runtime identity for the future staged rollout. This proves debug-provider wiring, not production App Attest or Play Integrity.

### 2026-07-16 — full server-side audio media validation deployed

- **Untrusted upload isolation:** the app now uploads only to `comprehension_audio_uploads/{schoolId}/{logId}.m4a`. Storage Rules allow only the parent who owns the authoritative log, require exact path/metadata/type/size, honour the recording kill switch, and close the path after a validated receipt exists. Clients cannot read or write the canonical `schools/{schoolId}/comprehension_audio/{logId}.m4a` object.
- **Bounded full decode:** `validateComprehensionAudioMedia` runs FFmpeg 6 in a 512 MiB, 30-second, concurrency-1/max-5 Gen2 worker. Input is forced through the MOV demuxer with only `file,pipe` protocols, no shell/stdin/network, one thread, a 15-second child-process timeout and bounded logs. The whole audio stream is decoded and re-encoded as mono 16 kHz/48 kbps AAC, metadata/video/subtitles/data are stripped, and server-observed duration/size limits are enforced. A header-only/truncated container and a 62-second fixture are rejected.
- **Least-privilege boundary:** the worker receives only base64 bytes—no school, child, log, bucket or UID—and runs as `lumi-audio-validator@lumi-ninc-au.iam.gserviceaccount.com`. That identity has zero project IAM roles and zero user-managed keys. Cloud Run invocation is private and bound only to `lumi-functions-runtime@lumi-ninc-au.iam.gserviceaccount.com`; an unauthenticated production probe returned HTTP 403.
- **Generation-safe receipt:** the privileged confirmation callable reads one pinned pending generation, applies a server-owned 20-attempt/15-minute UID rate limit, invokes the isolated decoder with an IAM ID token, create-only publishes the canonical object, and transactionally stamps exact canonical/source generations, validator version `ffmpeg-aac-mono-v1`, validated duration and SHA-256. Concurrent confirmations cannot overwrite each other; a prior orphan is reused only when every server-derived identity/integrity field matches. Playback signs the exact canonical generation. Invalid media deletes only the inspected generation; infrastructure failure leaves it for retry; pending residue is swept after 24 hours. Retention and account/student deletion cover both namespaces and every new receipt field.
- **Production rollout and canary:** Storage ruleset `1ca9ac53-19c6-4fe2-b552-7fa056069f5f` and all selected audio/storage/deletion functions are ACTIVE. Validator revision `validatecomprehensionaudiomedia-00002-cor` uses the isolated runtime identity with 512 MiB, 30 seconds, concurrency 1 and max 5; final generation-preconditioned confirmation revision is `confirmcomprehensionaudioupload-00003-zem`. A synthetic production canary returned HTTP 200, canonicalised a real M4A to 7,637 bytes, verified the server SHA/generation/duration receipt, removed the pending object, and then removed its temporary Auth user, tenant, objects and rate row in `finally`. Post-check: zero canary schools, zero canary rate rows; the pre-existing recording flag state was restored.
- **Dependency hardening:** `firebase-admin` is on 13.10.0 and `websocket-driver` is pinned to 0.7.5, removing the prior production critical advisory. `npm audit --omit=dev` reports 0 critical/high and 8 moderate; the remaining `uuid` chains are transitive Google-library paths not invoked with attacker-controlled UUID input.

### 2026-07-16 — P1 abuse, offline and CI hardening continuation

- **Public code abuse controls live:** `verifySchoolCode` and `verifyStudentLinkCode` now use server-owned durable counters keyed by hashed IP plus Auth UID where available. The limit is 30 attempts/hour and 100/day per dimension; an unidentifiable caller gets a stricter five/hour bucket. Counters contain no plaintext IP or submitted code, all direct client access is denied, and a Firestore TTL policy removes expired counter rows. Production invalid-code probes returned the intended 404/400 errors and created durable counters.
- **Credential lifecycle hardened:** school/student join credentials are generated with cryptographically secure randomness. New school/parent codes default to 30 days, co-parent invites to seven days, and portal validation caps requested lifetimes. Existing active credentials were capped to no more than 30 remaining days; the one active school code also has an explicit max-use ceiling. School-code redemption is now a server transaction with a hashed UID-bound receipt, so retries by one account consume exactly one use and a second account is rejected at the ceiling. Receipt expiry also has a Firestore TTL policy. An isolated production smoke test proved both properties and removed every temporary record.
- **Client credential mutation closed:** Firestore rules now deny all client create/update/delete operations on `schoolCodes`, `studentLinkCodes`, public attempt counters and redemption receipts. Generation, rotation, redemption and revocation must pass through tenant-authorised server code. The deployed Firestore ruleset is `ff5999b4-45b8-438e-a966-ed62e28d0c52`.
- **Security gates fail closed:** missing SMS rate-limit config now means enabled with a five/day cap; transaction/config failures return unavailable instead of silently allowing SMS. The only bypass is an explicit `emergencyFailOpen: true` operational override. Production `platformConfig/smsRateLimits` is enabled with five/day and fail-open false. Flutter reports the blocked/unavailable state instead of continuing to phone verification.
- **Release App Check guard:** `.dart_define.json` enables App Check and `scripts/flutter-build.sh` now refuses iOS/IPA/APK/appbundle release targets unless `LUMI_APP_CHECK_ENABLED=true`. School-code, student-link and SMS clients request limited-use tokens. Store-attested enforcement remains deliberately pending until Play Integrity/App Attest traffic can be observed.
- **Offline conflict policy implemented:** reading logs are create-once with random 128-bit IDs. If an offline queue finds that ID already on the server, the authoritative server record wins and refreshes the local cache; a future or manipulated device clock can never overwrite it. Feelings/comments stay in their separate field-specific merge paths, and permission denials remain parked and visible. The new future-clock regression passes.
- **Verification:** Functions build/lint and **132/132** tests pass; Firestore Emulator **147/147**; focused Flutter offline/SMS/linking **84/84** with clean focused analysis; school portal typecheck/build pass; super-admin typecheck/lint pass with zero errors; release-build guard shell validation and `git diff --check` pass. Seven selected Functions deployed with zero errors, Firestore rules deployed successfully, live invalid-code/SMS probes returned expected errors, durable counters appeared, and the isolated atomic-use canary cleaned up after itself. Fresh wrapper-built Android (119.0 MB) and unsigned iOS (76.2 MB) release artifacts contain the Play Integrity/App Attest fallback providers, retain native Analytics/Crashlytics defaults off, and pass history/artifact/private-key/service-account/demo-password scans. Android is intentionally still debug-certificate-signed and iOS unsigned until the organisation store accounts are available.
- **Keyless CI proven and old credential revoked:** a GitHub OIDC Workload Identity pool/provider is active and restricted to repository `nicplev/Lumi_Reading_Diary` on `refs/heads/main`; only that principal can impersonate `github-actions-admin`. `admin-deploy.yml` exchanges OIDC for short-lived Google credentials and invokes the pinned Firebase CLI without materialising a JSON key. PR #409's main-branch run `29492117424` passed WIF authentication, Cloud Build and live deployment in 7m58s; the admin login then returned HTTP 200. GitHub secret `FIREBASE_SERVICE_ACCOUNT_LUMI_NINC_AU` and key `556d…9af3` were permanently deleted. The deploy account now has zero user-managed keys. The earlier documentation incorrectly described this workflow as keyless while it still consumed that secret; this entry records the actual completed migration.
- **Auth verification quota bounded:** Identity Toolkit's effective `sendVerificationCodePerDayPerProject` limit was 4,000,000/day, which left distributed SMS/verification abuse effectively unbounded despite Lumi's five/day per-number gate. Quota preference `lumi-verification-codes-daily-cap` now has both preferred and granted value **500/day**. The existing enabled blocked-SMS and Auth-quota alert policies remain attached to both security recipients. Raise this cap only through reviewed change control when legitimate onboarding volume requires it.

### 2026-07-15 — P1-7 live credential and API-key hardening

- **Legacy admin credential revoked:** The untracked `school-admin-web/service-account.json` contained user-managed key `b7be…1e1b` for the retired `lumi-kakakids` project. Portal runtime code already uses ADC and the configured current-project credential path did not point at this file. The legacy key was disabled, permanently deleted from IAM and removed locally. A no-ignore fingerprint scan found no second workspace/cache copy. Current school-admin, super-admin and marketing endpoints still return HTTP 200.
- **Current Firebase keys restricted:** The iOS key now accepts only `com.lumi.lumiReadingTracker`. The Android key accepts only package `com.lumi.lumi_reading_tracker` signed by the current debug certificate. The browser key accepts the four AU Hosting sites and their `.firebaseapp.com` mirrors, `lumi-reading.com`, the reserved `www` hostname and local development. Positive Identity Toolkit probes returned HTTP 200; an attacker referrer, wrong iOS bundle and wrong Android certificate each returned HTTP 403. All three keys retained their existing 27-service Firebase API-target allowlist.
- **Android release blocker:** `android/app/build.gradle.kts` still signs `release` with the debug key and Firebase has no Play signing fingerprints registered. Before any Play release, configure a protected upload/release signing setup, add the Google Play App Signing SHA-1 to Firebase and the Android API-key allowlist, then repeat the signed-device positive/negative test. Restricting the key only to the known certificate is safe for today's build but intentionally does not pretend a future Play-signed binary is covered.
- **Books key contained:** Current source and local environments contain no `GOOGLE_BOOKS_API_KEY`, and the obsolete project's separate `BOOKS API` key matches neither current nor legacy local Firebase keys. Monitoring showed only six Books API responses across the sampled 90 days with no credential label. The key is now API-target-restricted to `books.googleapis.com`; a Books request succeeds and a non-Books request is rejected with HTTP 403. Confirm no supported legacy app still needs it before deleting it entirely or replacing direct mobile use with a server proxy.
- **Management-plane change:** `apikeys.googleapis.com` was enabled in both projects solely to administer these restrictions. No application data path or client API surface was added.
- **Artifact rescan:** Gitleaks 8.30.1 originally rescanned all 566 commits plus the unsigned release `Runner.app` bundle and found no leak. On 17 July the current run covered **612 commits**, a fresh 119.1 MB release APK and a fresh 76.4 MB unsigned `Runner.app`. The scanner initially reported a misleading zero-byte pass for the compressed APK; `scripts/security/scan-secrets.sh` now extracts APK/AAB/IPA containers and refuses empty packages. The corrected scan inspected 1,105 APK files plus the iOS bundle and found no Gitleaks or private-key/service-account/demo-password marker. Final store-signed AAB and IPA scans remain required.
- **Still open:** scan a final signed IPA/store bundle, repeat Firebase/Auth smoke tests from signed iOS and Android devices, register the Play App Signing certificate, and evidence Firebase Auth/API quotas and abuse alerts. These are why P1-7 remains partial rather than complete.

### 2026-07-15 — production recovery, budget and ADC hardening

- **PITR and deletion protection live:** The default Firestore database in `australia-southeast1` now has `POINT_IN_TIME_RECOVERY_ENABLED`, a seven-day (`604800s`) version-retention window and database deletion protection. This protects against accidental database removal and enables point-in-time clones; it does not replace application-level safeguards against document deletion.
- **Restore drill passed:** A clone named `restore-drill-20260715` was created from the 05:44 UTC production snapshot. The operation ran from 05:49:31 to 06:02:30 UTC (**12m59s**). Source and restore counts matched exactly: 5 schools, 52 students, 14 classes and 714 reading logs. The isolated clone was deleted at 06:04 UTC; only `(default)` remains. This proves Firestore recovery for the sampled collections with a roughly five-minute recovery point, not a full cross-service disaster-recovery exercise.
- **Budget alert live:** Billing budget `Lumi production monthly early warning` is scoped only to project number `3795320704`, recurs monthly at **AUD 100**, and alerts the existing billing administrator at current spend 50%, 90%, 100% plus forecasted spend 100%. It is alert-only and does not cap or stop services. Cloud Monitoring currently has zero notification channels and zero alert policies; choose and verify an incident recipient before adding high-signal function-error/auth-spike/deletion-job policies. Per-service anomaly dashboards and alert policies remain open under P2-1.
- **Super-admin moved to ADC:** `admin/src/lib/firebase-admin.ts` and the admin deployment workflow no longer accept or write `FIREBASE_SERVICE_ACCOUNT_KEY`. A real custom-token login returned 200, the server verified `/superAdmins`, and `/api/dashboard` returned 200 with its full payload. The duplicated current-project key `9dd4…884c` was then disabled, deleted and removed from local files; the same authenticated dashboard call still returned 200 after revocation. PR #396 passed CI/Gitleaks and its keyless automatic deployment completed as active Cloud Run revision `ssrlumidevadminau-00061-viq`; the obsolete GitHub runtime-key secret was then deleted. The public/login endpoints remain healthy and the protected dashboard returns 401 without a session. A no-ignore fingerprint scan found no copy in the workspace/key directory. Redacted artifact triage found only the public Firebase browser identifier and Next's generated server-side preview/action signing keys in protected server manifests—not the revoked Admin credential.
- **App Check enforcement remains unsafe:** The three managed service modes are explicitly `UNENFORCED`. Thirty-day metrics contain no valid app ID and show about 85.9k Firestore, 891 Auth and 37 Storage requests as invalid/missing/unknown while still allowed. Enforcing today would cause an outage. Produce an App-Check-enabled canary build, register real debug/Apple/Play/web attestations and observe valid traffic before staged enforcement.
- **IAM audit closed:** Workload runtimes are split across dedicated keyless identities and both default-account `Editor` bindings are gone. The GitHub admin deploy identity keeps deployment-control roles but no longer has project-wide Service Account User or Secret Accessor; it can act as only the three named runtime identities and has metadata-only Viewer on the admin-session secret. Re-audit deploy roles after the next WIF workflow run, but runtime least privilege is production-proven.

### 2026-07-15 — P1-3 analytics privacy implementation

- **Runtime defaults:** Android now ships `firebase_analytics_collection_enabled=false`, `firebase_crashlytics_collection_enabled=false`, ad-ID collection false and ad-personalisation false. iOS ships the equivalent Analytics and Crashlytics collection flags false. Dart also defaults both preferences false and applies advertising consent as permanently denied. This is defence in depth against collection before the Flutter UI loads.
- **Adult controls:** The shared parent/staff **Settings → Account → Privacy & diagnostics** screen has independent product-usage and crash-report switches. Choices are device-local and reversible. Analytics withdrawal removes legacy UID/role properties and resets the app-instance ID; Crashlytics withdrawal clears the user identifier and deletes unsent reports.
- **Data minimisation:** The splash flow no longer sends the Firebase UID or role to either SDK. Analytics retains only coarse event names and drops child feeling, book count, reading minutes, badge type, streak count, failure reasons and export counts. Crash calls are suppressed unless collection is enabled.
- **Disclosures:** The public privacy-policy source, `docs/app-store/app-privacy-labels.md` and `ios/Runner/PrivacyInfo.xcprivacy` now match. Labels remain conservatively "Linked to You" because pseudonymous Firebase installation identifiers may identify a device, even though no Lumi account UID is attached.
- **Voice default audit:** `ComprehensionRecordingSettings.defaults()` is false and missing school settings parse false. Production has 5 school documents; 1 legacy/demo school has an enabled value without current authority evidence. The first-enable authority gate treats that value as disabled and will require an admin decision; it must not be silently grandfathered or given invented evidence. The platform-wide kill switch remains a separate emergency control.
- **Verification:** diagnostics preference/UI tests **8/8 passed**; targeted analysis has no issues; full analysis has no errors/warnings and the same **123 existing info lints**; school portal TypeScript and Next production builds pass. Clean Android and unsigned iOS release builds succeed. The built Android manifest has Analytics, Crashlytics, advertising-ID collection and ad-personalisation all false; the built iOS app has both collection flags false and a valid privacy manifest. PR **#393** passed required CI and was squash-merged as `6766e63`. The school portal deployed successfully to Node 24 revision `ssrlumischooladminau-00066-yec`; the live policy returns the new off-by-default, withdrawal and no-account-UID text. A release-device traffic capture before opt-in and after each opt-in/withdrawal is still required before P1-3 can be closed.
- **Resume point:** Perform the release-device network capture, ship the mobile changes in the next signed app release, and update the actual App Store Connect / Play Console questionnaires. Do not mark P1-3 complete from source inspection alone.

### 2026-07-15 — audio security local verification completed

- **Coverage added:** `functions/test/comprehension_audio.integration.test.js` exercises the deployed handlers against emulated Firestore/Storage; `functions/test/comprehension_audio.http.integration.test.js` crosses the real emulated Auth token + callable HTTPS boundary; `functions/test/comprehension_audio.appcheck.integration.test.js` proves an authenticated request without App Check is rejected when enforcement is enabled; `test/services/comprehension_audio_service_test.dart` verifies the Flutter callable adapter. Reusable npm scripts are `test:audio:integration`, `test:audio:http` and `test:audio:appcheck`.
- **Security cases passing:** unauthenticated, disabled-feature, wrong-parent, unassigned-teacher and cross-school calls are denied; hostile stored paths cannot redirect playback/deletion/retention; canonical owner uploads succeed; MIME-only junk and metadata mismatches are rejected and deleted; direct Storage reads remain denied; cleanup quarantines legacy mismatches.
- **Bugs found and fixed by the deeper run:** scheduled cleanup could delete data and then fail its audit write because `performedByEmail` was `undefined` (now stored as `null`); the local Firebase CLI 13.35 was incompatible with `firebase-functions` v7 (upgraded to 15.23); audio Firestore sentinels now use the modular `FieldValue`/`Timestamp` imports so the isolated Functions worker does not fail after a destructive operation. Emulator scripts now default to Java 21.
- **Real file check:** a genuine AAC `.m4a` was generated locally with macOS `afconvert`, confirmed by `afinfo` (M4A container, AAC, 48 kHz, about 1.5 seconds), and accepted by `hasIsoMediaFtypSignature`. This proves compatibility with a real M4A header, **not** full decoding or playable-content validation.
- **What local emulators cannot prove:** production `iam.serviceAccounts.signBlob`/signed-URL IAM, genuine Apple/Android App Check attestation, deployed rule parity, physical microphone codecs/permissions/playback, or a decoder/transcoder that has not yet been implemented.
- **Staging prerequisite:** `functions/src/global_options.ts` intentionally pins production Gen2 functions to `lumi-functions-runtime@lumi-ninc-au.iam.gserviceaccount.com`. A separate staging project still needs a parameterised per-project identity (or an explicitly created staging account with equivalent narrow grants). Do not point a staging deploy command at production by accident.
- **App Check replay protection implemented and canaried:** all three Flutter audio callable requests use `HttpsCallableOptions(limitedUseAppCheckToken: true)`. When enforcement is enabled, the server consumes tokens and explicitly rejects `request.app.alreadyConsumed`. Unit coverage proves replay rejection while enforced and preserves the intentional enforcement-off behavior; physical debug-provider traffic passed during the temporary production canary. Store-signed App Attest/Play Integrity evidence remains under P1-1.

### 2026-07-17 — first-opt-in school audio authority and retention gate

- **Trigger:** The gate appears only when a school administrator first turns comprehension recording on. It is not shown during school signup and schools that keep audio off never see it. A legacy/outdated authority version also requires reconfirmation rather than being grandfathered.
- **Recorded decision:** The admin must confirm they are authorised by the school, confirm the school will notify families of purpose/retention and provide a practical opt-out, and choose 7, 30, 90 or 365 days (30 recommended). The server records the admin UID/role, fixed declaration version, timestamp, both declarations and retention choice in protected school settings plus `adminAuditLog`. The modal expressly says this is governance evidence, not legal advice or a substitute for required notice/consent.
- **Enforcement:** The school setting is server-owned in Firestore Rules. Storage uploads and the upload-confirmation Function require the exact current authority version, timestamp, allowed retention choice, school enablement and the existing platform kill switch. Missing/outdated evidence fails closed. The scheduled deletion worker applies each school's selected period; the global 90-day value remains only a legacy fallback, and unconfirmed uploads still have the separate 24-hour fail-safe.
- **Verification:** Portal TypeScript and authority tests **4/4**, focused Functions tests **11/11**, Firestore Rules **152/152**, Storage Rules **14/14** and audio integration **8/8** pass. Tests deny direct setting forgery, school upload without evidence and server confirmation with outdated evidence. The new collection-scope retention index is declared in `firestore.indexes.json`.
- **Production rollout:** The school portal is live on `ssrlumischooladminau-00072-yap`; audio confirmation is on `confirmcomprehensionaudioupload-00009-vef`; cleanup is on `cleanupcomprehensionaudio-00012-siz`. Firestore ruleset `ebc3bae6-9919-4ca7-b71a-fcbfbb8fa283` and Storage ruleset `0033c14a-0038-478c-8043-868bd42746b4` exactly match reviewed source. Index `CICAgJjFx5sK` is READY. The scheduled worker is enabled with a 90-day legacy fallback; a pre-enable aggregate dry run found zero audio rows old enough for immediate deletion. All current schools have zero current authority records, so the one legacy-enabled demo school fails closed and must complete the modal before new uploads. The post-deploy 17-job Scheduler IAM audit passes, and the live login/privacy pages return HTTP 200 with the retention disclosure present.
- **Operational caution:** The existing enabled legacy/demo school has no current evidence and is intentionally not migrated. After deployment it will be treated as off until its administrator completes the modal. Do not fabricate a real school's authority decision during migration.

### 2026-07-15 — P1-2 account/student deletion completed

- **Status:** P1-2 is implemented and verified. A shared **Settings → Account** screen now exists for parents and teachers. Both roles can permanently delete their own account; assigned teachers can also select and delete a student from one of their authoritative classes. School admins retain the equivalent portal workflow. Parents are told explicitly that account deletion does not erase the school's child record and must coordinate child-record erasure with the school.
- **Server workflow:** `functions/src/deletion.ts` owns deterministic, idempotent jobs, recent-auth and typed-confirmation checks, class/school authorisation, five-attempt leased retries, sanitized requester-only status, and 90-day minimal completion receipts followed by automatic purge. Direct client reads/writes of `deletionJobs`, deletion-marker writes and student deletes are denied by Firestore rules.
- **Account result:** removes Firebase Auth, all located parent/staff memberships and membership notifications, user/index/feedback/token-adjacent data, guardian links, authored comments, voice recordings and direct attribution. Core school reading events are retained only in de-identified form so deleting a parent's login does not silently destroy the child's educational history. Staff roster/author references are removed or de-identified. The portal's existing 24-hour staff delete/undo markers migrate into this same engine when due.
- **Student result:** removes the student doc/subcollections, reading logs/comments/audio, parent links, class/group/allocation/campaign references, notifications and link codes; guardian Auth accounts are never deleted. The portal hides/decrements a queued student atomically and the worker avoids double-decrementing.
- **Verification:** Functions build/lint pass; helper/full Functions tests **116/116**; dedicated Firestore/Auth/Storage cascade integration tests **2/2**; Firestore rules **136/136**; deletion Flutter service/widget tests **7/7**; targeted changed-file analysis has no issues; portal TypeScript check and production build pass. Full `flutter analyze` still reports only the pre-existing 123 info-level lints.
- **Deployment:** `requestAccountDeletion`, `requestStudentDeletion`, `getMyDeletionStatus` and the updated `processPendingUserDeletions` are active in `australia-southeast1`. Unauthenticated production probes return 401. The insecure legacy `deleteStudentWithCascade` callable was retired after the replacements became active and now returns 404.
- **Resume point if interrupted:** P1-2 needs the user's noted signed-in account-deletion device retest and staged App Check rollout only; do not re-implement or redeploy the workflow. Continue with P1-3/P1-4 device evidence, then the store-blocked controls.

### 2026-07-15 — AI comprehension evaluation Phase 1 deployed inert

- **Scope:** Implemented and deployed only the inert security boundary from `docs/AI_COMPREHENSION_EVAL_CHECKLIST.md`. No STT/LLM SDK, provider call, entitlement or background worker was added.
- **Data boundary:** Evaluations live at `schools/{schoolId}/comprehensionEvals/{logId}`, separate from parent-readable reading logs. Parents cannot read them; assigned teachers must prove `classId` scope; school admins and live read-only impersonation sessions may read; all client writes are denied.
- **Server-only state:** Direct client access is denied for `aiEvalJobs`, `aiQuestionClassifications`, `aiEvalOpsConfig`, and `schools/{schoolId}/adminMeta`. School clients cannot write `settings.aiEvaluation` or the signed-in-readable `platformConfig/aiEvaluation` kill switch. Parent edits to server-captured `comprehensionQuestionText` remain denied by the reading-log update allowlist.
- **Verification:** `cd functions && npm run test:rules` → **145 passed, 0 failed**. Coverage includes own-child parent denial, cross-class denial, class-filter query provability, admin/impersonation reads, settings/adminMeta denial and server-collection write denial. Production index comparison: **60 remote, 0 missing locally**. All six newly pending composite indexes and all four eval-retention field-index variants reached **READY** before rules activation. Active Firestore ruleset `9c65ac25-4a52-46a9-902a-115a2d5fcc34` has the same local/remote SHA-256: `2698760bf82dad3fa20d609d5201f0b5897e162f48eca0587978dc1e8f502824`.
- **Fail-closed state:** Production now has exactly `platformConfig/aiEvaluation {enabled:false}`. Anonymous probes of AI jobs/cache/config return 403. There is still no AI processing code or provider secret deployed.
- **Security gates still open:** Complete the PIA/notice/APP 8 provider work and STT residency go/no-go before sending any child content externally. Keep AI disabled by default. The existing hardcoded production Functions service account still blocks a safe separate-project staging deploy.
- **Resume point:** Continue in `docs/AI_COMPREHENSION_EVAL_CHECKLIST.md` at Phase 0. Do not begin provider-connected Phase 2/3 work until the privacy/go-no-go prerequisites are satisfied.

### 2026-07-15 — AI comprehension Phase 0 Australian STT spike

- **Cloud configuration:** Enabled `speech.googleapis.com` in `lumi-ninc-au` and granted the existing Australian Functions runtime service account `roles/speech.client`. No Anthropic secret, LLM dependency, AI worker or entitlement was added; the production kill switch remains exactly `{enabled:false}`.
- **Regional evidence:** Speech-to-Text V2 requests reached `australia-southeast1`. Google Locations metadata reports `en-AU` `long`/`short` support. A synthetic 6.23-second AAC/M4A was transcribed correctly by `long` without transcoding; `latest_short` returned no transcript for that clip but did transcribe most of a 1.35-second sample. Chirp 2 returned unavailable for the Australian region.
- **Cost/capacity evidence:** Official V2 billing and observed requests use one-second upward rounding (1.35 s → 2 s; 6.23 s → 7 s). Live regional synchronous-recognition quota is 211 requests/minute versus planned `maxInstances=5`; this is enough for the spike, not a substitute for peak load testing.
- **Safety evidence:** Added a synthetic-only adversarial transcript fixture covering injection, prompt exfiltration, off-topic answers, adult prompting, gibberish, empty speech, insufficient evidence and incidental personal information, plus an automated schema/coverage test.
- **Repository verification:** Functions build and `npm run test:functions` pass **118/118**; lint has zero errors and the same eight existing non-null-assertion warnings. JSON validation and `git diff --check` pass.
- **Repository reconciliation:** The isolated evidence/fixture slice is PR #391 from `ai/phase0-go-no-go`; squash-merge only after required CI passes.
- **Decision:** Conditional technical GO for the Australian `long` model path; **NO-GO for production/school processing**. No child audio was tested. Representative authorised recordings, teacher accuracy review, approved PIA/notice/opt-out/no-backfill rules, Anthropic DPA/APP 8/retention/ZDR and cost controls remain release blockers. Full evidence and the working PIA are in `docs/AI_EVALUATION_PLAN.md`.
- **Resume point:** Finish the open Phase 0 gates in `docs/AI_COMPREHENSION_EVAL_CHECKLIST.md`. Do not deploy a provider-connected pipeline or set the kill switch true.

### 2026-07-15 — incremental statistics audit, repair and production reconcile

- **Production audit:** `platformConfig/incrementalAggregation` already had `studentStats:true` and `classStats:true`. All 52 sampled production students had `stats.readingDates`; all 14 classes had `stats.activeStudentIds`. Since the flag update, each hot-path function handled 1,089 HTTP 200 event deliveries with zero error entries.
- **Backfill defect found:** The 12 July scheduled reconcile logged one per-class `INVALID_ARGUMENT` while still recording overall completion. The class query combined 30 student IDs with two statuses, producing 60 normalized disjunctions above Firestore's maximum of 30. The cursor still wrapped to null, so that class would remain stale until another successful pass.
- **Fix:** Both legacy and self-heal class recomputation now share 15-ID batches. Incremental removal checks also convert the school-local reading date to an exclusive UTC range using the school timezone, including Melbourne/Sydney DST transitions, instead of treating local midnight as UTC.
- **Verification:** Functions build and tests pass **123/123**; lint has zero errors and the same eight existing warnings; Firestore rules pass **145/145**; focused Flutter offline queue tests pass **34/34**. Regression tests cover the compound-query budget, Melbourne 23/25-hour DST days, a Sydney summer day and invalid-timezone fallback.
- **Production result:** `aggregateStudentStats`, `updateClassStats` and `reconcileStatsScheduled` were updated in `australia-southeast1`. A controlled scheduled reconcile then processed **52 students and 14 classes with zero error entries**; the student/class cursors both returned to null. P1-5 is complete.
- **Repository reconciliation:** Branch `security/stats-reconcile-disjunction`, PR #392. Squash-merge only after required CI passes.
- **Offline completion evidence:** Server receipt timestamps, date bounds, write allowlists, idempotent IDs, server-wins/create-once conflict handling and parked permission-denied UI state exist and are tested. The combined airplane-mode → access-revocation → reconnect scenario passed on a physical iPhone with synthetic data; the rejected write never appeared on the server.

---

## Current implementation verdict (plain English)

Lumi's highest-risk access-control findings are now remediated, covered by emulator tests and active in production: teachers are class-scoped, logs/comments are bound to authoritative child/class records, system fields are immutable, audio paths are canonical, uploads are owner-scoped, onboarding is server-owned, and client role/provisioning footguns are removed. Rules still default to deny, roles live in protected membership docs, audio reads remain callable-only, and the deployed security Functions run in Australia.

Lumi is still **not ready to hold production children's data** until the remaining controls are completed and proven in the live project. The main release blockers have shifted to store-attested App Check rollout, live store privacy disclosures, Play signing and documentary school/voice authority. Physical student and account deletion now pass with full production cascade evidence. Offline revocation, optional-diagnostics consent traffic, audio authorization, full decode/transcode validation and direct-install iOS parent/Android teacher UI paths are production-proven; the remaining attestation device gate is store-signed App Attest/Play Integrity evidence.

| Area | Status | One-line |
|---|---|---|
| Firestore rules | 🟢 repo / 🟢 live | 147 emulator tests pass; active production source hash exactly matches the reviewed local rules. |
| Cloud Storage | 🟢 rules / 🟠 rollout | Pending/canonical isolation and full decode are live; debug App Check/device UI paths passed, while store attestation remains. |
| Children's privacy | 🟠 | Account/student deletion is deployed and optional-diagnostics consent traffic passed on iPhone; school authority and live store disclosures remain. |
| App Check | 🟠 | Wired into every sensitive fn, enforcement defaults off everywhere. |
| Offline sync | 🟢 | Server receipt/date bounds, durable queue, server-wins conflicts, AU/DST stats and physical access-revocation reconnect are covered. |
| Scaling & cost | 🟡 | Incremental aggregation and a production budget alert are active; broader load/cost evidence remains. |
| Ops | 🟠 | Force-update, opt-in crash reporting, PITR/deletion protection and a tested restore exist; incident readiness/IAM reduction remain. |

No anonymous Firestore/Storage data path was found in the active rules. Public enquiries now use a validated, rate-limited callable. The remaining risk is primarily **privacy, live-control verification, credential operations, offline correctness and operational readiness**; for children's data those gaps remain release-blocking.

---

## What's already solid (don't re-litigate)

- [x] **Roles are server-side, not client-writable.** Role from `schools/{id}/users/{uid}.role`; self-update pins `role`/`schoolId` (`firestore.rules:308-309`); teacher self-provisioning removed (#383). Document-roles instead of custom claims is fine *because the field is locked*.
- [x] **Subcollections have explicit rules** (`comments`, `notifications`, `readingLevelEvents`) — no reliance on inheritance.
- [x] **Paid-access gate fails closed** — `studentAccessLive` (`firestore.rules:134-138`) denies on missing `access`.
- [x] **Audio read is denied** (`storage.rules`) and the app fetches via the class-authorised `getComprehensionAudioUrl` callable, which derives the canonical path and signs only its validated generation. *(Local integration, production signer IAM and a temporary enforced debug-provider canary are proven; store-attested rollout remains under P1-1.)*
- [x] **Streaks are server-authoritative** (`functions/src/streak_refresh.ts`, `dateUtils.ts`); client `DateTime.now()` is display-only.
- [x] **No admin keys or ad/attribution SDKs in the app.** `firebase_options.dart` = public client keys only; no AdMob/AppsFlyer/Adjust/Facebook/Segment; SendGrid via Firebase secret params.
- [x] **Rules default-deny and are in Git**; `firebase.json` points at the active files.

---

## P0 — Release-blocking (before any real child data or external beta)

### `[x]` P0-1 · Audio signing/deletion trusts a client-editable path (confused deputy)
- **Severity:** Critical
- **Why:** `getComprehensionAudioUrl` signs a read URL for whatever path is stored on the log doc, after only a same-school role check — it never checks the path is inside that school's folder. Because the log's audio-path field is editable (P0-2) and signed URLs bypass Storage rules, a teacher can point it at another object (incl. `schools/{otherSchool}/comprehension_audio/...`) and read another school's child-voice recording.
- **Where:** `functions/src/comprehension_retention.ts:368,381` (sign), `:~260` (delete), `:~100-120` (scheduled cleanup); enabled by editable field at `firestore.rules:562-568`.
- **Fix:**
  - [x] Keep comprehension audio opt-in and kill-switch controlled until the server boundary ships and passes tests. *(The hardened boundary is live; production is globally available but enabled for one explicitly opted-in school, and the synthetic demo tenant was restored off.)*
  - [x] Never read the object path from the doc for a privileged op. Derive it: `const expectedPath = \`schools/${schoolId}/comprehension_audio/${logId}.m4a\`;`
  - [x] Add App Check enforcement to all audio callables. *(Wired behind `COMPREHENSION_AUDIO_APP_CHECK_ENFORCED`; a temporary physical-device canary passed for confirm/play/delete plus missing-token denial, then enforcement was safely restored off. Store-attested broad rollout belongs to P1-1.)*
  - [x] Make the audio fields (`comprehensionAudioPath`, `comprehensionAudioUploaded`) server-owned.
  - [x] In cleanup, reject any legacy row whose stored path ≠ the canonical path before deleting.
- **Verify:**
  - [x] Test: a teacher who rewrites `comprehensionAudioPath` to another school's path gets denied / a URL only for their own school's object. *(Handler integration proves playback and deletion select only the canonical School-X path; real callable HTTP integration proves a School-Y Auth token is denied.)*
  - [x] Test: "School X cannot access School Y, including through an injected audio path" (see test matrix). *(Storage 13/13, handler integration 7/7, callable HTTP 4/4.)*
  - [x] Live production signer: an assigned demo teacher mints a 15-minute canonical signed URL and reads a valid media header; an unassigned teacher, parent and unauthenticated caller are denied. *(Synthetic demo data only; temporary identities fully removed.)*
### `[x]` P0-2 · Reading-log writes permit mass assignment / forged stats
- **Severity:** High
- **Why:** Update only re-checks minutes — an owner or any school teacher can later change `studentId`, `classId`, `parentId`, `createdAt`, date, status, validation and audio fields. The old `parentId` still passes the ownership test while the new child/date/status corrupts another student's totals and streak. Create doesn't pin `schoolId` to the path, bind `classId` to the student's real class, restrict fields/types, or require server time.
- **Where:** create `firestore.rules:531-540`; update `:562-568`; stats consume supplied fields at `functions/src/stats_aggregation.ts:109-122`, `:278-348`; client writes `DateTime.now()` in `lib/services/reading_log_service.dart`.
- **Fix:**
  - [x] **Update:** split into narrow ops with an allow-list + immutable identity/system fields:
    ```rules
    request.resource.data.diff(resource.data).affectedKeys()
      .hasOnly(['minutesRead','bookTitles','feeling','notes','updatedAt'])
    && request.resource.data.studentId == resource.data.studentId
    && request.resource.data.classId  == resource.data.classId
    && request.resource.data.parentId == resource.data.parentId
    && request.resource.data.createdAt == resource.data.createdAt
    && request.resource.data.updatedAt == request.time
    ```
  - [x] **Create:** allow only a documented schema; check types/enums/sizes; require `schoolId == schoolId`; load the student and require the real class; use `request.time` for receipt fields. *(Parent and teacher-proxy branches now share one schema evaluation, avoiding the 1,000-expression ceiling; optional lists/strings/maps/booleans/timestamps are bounded and malformed cases are denied.)*
  - [x] Keep validation / aggregate / access / audio fields server-only.
  - [x] Use a random idempotency UUID as the doc ID, not a millisecond timestamp. *(128-bit `Random.secure()` hex ID; 100-ID uniqueness/shape regression passes.)*
- **Verify:**
  - [x] Test: parent cannot change `studentId`/`classId`/`parentId`/school/author/createdAt/status/validation/stats/audio fields.

### `[x]` P0-3 · Teachers have school-wide child access, not class access
- **Severity:** High
- **Why:** Teacher reads/writes are gated on "is a teacher at this school?", not "does this teacher teach this child?". A Class-A teacher can query and alter Class-B children, logs, comments, allocations and groups. Class docs themselves *do* check `teacherId`/`teacherIds` — the pattern just isn't applied elsewhere. Conflicts with the privacy policy ("that child's teachers", `school-admin-web/src/app/legal/privacy/page.tsx:110-113`).
- **Where:** students `firestore.rules:418-447`; logs `:516-568`; comments `:597-634`; allocations `:628-635`; reading groups `:649-653`. Correct pattern to copy: `:475-496`.
- **Fix:**
  - [x] Use `teacherTeachesClass()` for every teacher read/write involving a child; bind each student/log/comment/allocation/group to a class and verify it. Admins keep school-wide access.
  - [x] Update teacher **queries** in the app/portal to include an allowed `classId` condition (rules aren't query filters — an over-broad query fails closed once the rule tightens). *(Teacher screens/services audited; history, dashboard, student/group detail and library assignment listeners are class-scoped. School/super-admin portals intentionally retain school-wide access.)*
- **Verify:**
  - [x] Tests: Class-A teacher cannot get/list/update/delete Class-B students/logs/comments/allocations/groups; School-X staff cannot reach School-Y; admin retains whole-school access. *(Firestore Emulator 131/131; audio callable remains tracked under P0-1.)*

### `[x]` P0-4 · Teacher proxy log doesn't prove the child is in the class
- **Severity:** High
- **Why:** Proxy-log create proves the teacher teaches the submitted `classId`, but never that the submitted `studentId` belongs to that class — so a teacher can pair their own class ID with any child in the school. The existing negative test only checks a *wrong* class ID, not this mismatched pair.
- **Where:** `firestore.rules:551-560`.
- **Fix:**
  - [x] Load the student and require `student.classId == request.resource.data.classId` (and/or membership in the class's `studentIds`).
- **Verify:**
  - [x] Test: proxy log denied when child and class don't match. *(Firestore Emulator 127/127.)*

### `[x]` P0-5 · Storage uploads/overwrites authorised only by sign-in
- **Severity:** High
- **Why:** Any authenticated account can create/overwrite any community cover, or drop/replace any school's comprehension recording (<2 MB, `audio/*`). MIME type is user-controlled metadata, not proof of content. Storage requests are independent of Firestore, so "enforced app-side" doesn't hold. Current Storage tests *assert* this broad behaviour and have no cover / cross-tenant cases.
- **Where:** `storage.rules:7-24` (covers), `:44-61` (audio).
- **Fix:**
  - [x] Prefer a callable / signed-upload flow that derives a canonical path after checking the log + owner. *(The client writes only an owner/log-bound pending object. The callable derives both paths, fully decodes the pinned source generation, and alone publishes the canonical object and receipt.)*
  - [x] If keeping client upload: rules must consult Firestore membership/log, permit only the linked parent who owns that log, check the exact filename, use create-only where possible, and make teachers class-scoped.
  - [x] Enforce the comprehension kill switch server-side; validate real media after upload and delete rejected objects. *(Kill switch, metadata/type/size/signature and full bounded decode/transcode are server-enforced. Invalid media is removed generation-safely; infrastructure failures remain retryable.)*
- **Verify:**
  - [x] Tests: authenticated outsider cannot upload/overwrite covers or any school's audio; disabled-audio flag blocks upload + URL mint + processing. *(Storage 13/13, handler integration 7/7, callable HTTP 4/4 and a live synthetic production decode canary pass.)*

### `[x]` P0-6 · Comments not bound to their parent reading log
- **Severity:** High
- **Why:** Comment access trusts denormalised `studentId`/`parentId`/`authorRole`/`logId` independently instead of loading the parent log and comparing. A linked parent can comment under another known log while supplying their own child's ID; a teacher can post under any school log and target denormalised parent data (which feeds notifications).
- **Where:** `firestore.rules:597-622`; mirror/notify in `functions/src/index.ts`.
- **Fix:**
  - [x] Bind every comment to an existing log: same child, same school/class, expected parent recipient, caller ID, bounded body, allowed keys, `createdAt == request.time`. Apply the same class-teacher check.
- **Verify:**
  - [x] Test: comment denied when its child/parent doesn't match the containing log. *(Also denies extra fields; Firestore Emulator 131/131.)*

### `[x]` P0-7 · Vulnerable production dependencies in Cloud Functions
- **Severity:** High
- **Why:** `npm audit --omit=dev` in `functions/` (15 Jul 2026) = **22 advisories: 2 critical, 7 high, 12 moderate, 1 low**. Critical chains: `fast-xml-parser`, `protobufjs`; high: `@grpc/grpc-js`, Axios, `jws`, `node-forge`, Express/path-matching, `form-data`. Internet-facing privileged functions shouldn't ship an untriaged critical tree.
- **Where:** `functions/` dependency tree.
- **Fix:**
  - [x] Upgrade supported Firebase/Admin + direct packages; review breaking changes; rebuild. *(Firebase Admin is now 13.10.0; `websocket-driver` is pinned to the patched 0.7.5. The remaining 8 moderate `uuid` findings are transitive Google-library paths not invoked with attacker-controlled UUID input.)*
  - [x] Re-run `npm audit`; run all function/rules tests; canary deploy. *(Audit + tests/build pass. The refreshed bundle was canary-deployed through security Functions in Phase 1 and the statistics Functions in P1-4/P1-5; all are active.)*
  - [x] Re-run the portal audit with a working registry (pnpm endpoint returned HTTP 410 — not "clean"). *(Generated a temporary npm lock for audit compatibility, upgraded Next 15.5.7 → 15.5.20, and rebuilt successfully.)*
- **Verify:**
  - [x] `npm audit --omit=dev` shows 0 critical/high (or documented, unreachable exceptions); full suite green. *(Functions: 8 moderate, 0 high/critical; portal: 2 moderate, 0 high/critical.)*

### `[x]` P0-8 · Remove permissive-rule instructions and release provisioning/test code
- **Severity:** High (dangerous production capability) / Medium (test-cred)
- **Why:** A forgotten temporary `allow read, write: if request.auth != null` would expose the whole DB. A hidden in-app "Create Admin Account" workflow + client-side test-data seeding is a dangerous release capability (currently gated by the dev allow-list + bootstrap conditions, so **not** a confirmed unauth takeover, but it can create orphan Auth accounts). Checked-in demo/review passwords aren't made safe by nightly rotation.
- **Where:** `FIRESTORE_RULES_TESTING.md` (permissive-rule how-to); `lib/screens/auth/login_screen.dart:564-630`, `:1343-1369`; `lib/utils/setup_test_data.dart`; `scripts/seed_demo_school.js:151` + the other review-account default password.
- **Fix:**
  - [x] Rewrite `FIRESTORE_RULES_TESTING.md` so tests use only the emulator; delete the deploy-permissive-rules procedure.
  - [x] Strip test/provisioning code from release builds; do provisioning only in an audited server/admin tool. *(Removed `TestDataSetup`, the hidden dev admin creator, the public registration route, and the demo-request jump into client provisioning. Demo requests now stop for staff review.)*
  - [x] Generate random initial demo/review passwords via secret/interactive input; rotate existing; never print in CI logs. *(Seed/restore scripts require ≥16-character environment/CLI secrets; checked-in defaults and password output were removed.)*
  - [x] Archive / clearly mark non-deployable the stale `firestore.rules.production`, `firestore.rules.nested`, `firestore.rules.backup`, `firestore.indexes.json.backup`, `firestore.indexes.nested.json`.
- **Verify:**
  - [x] Release build has no admin-creation path; grep for permissive-rule string returns only emulator context. *(Release APK built successfully; binary scan confirms the client-provisioning symbols/routes and old passwords are absent. The sole permissive-rule literal is the checklist's historical finding.)*

---

## P1 — Before store submission / production launch

### `[~]` P1-1 · Enable + enforce App Check; add durable rate limits + abuse alerts
- **Severity:** High (control gap)
- **Why:** `firebaseConfig` is public; App Check is the control that raises the cost of scripted abuse. It's wired everywhere but enforcement defaults off, and several public paths are unprotected: `verifySchoolCode` is callable without auth + no durable rate limit; SMS helper defaults disabled + fails open on internal errors; anonymous `schoolOnboarding` writes have no rate limit / allow-list / size cap / `createdAt == request.time`; any signed-in account can create unlimited tenants then self-bootstrap as admin.
- **Where:** `lib/core/services/app_check_service.dart:25-30`; `scripts/flutter-build.sh:34-36` (+ `.dart_define.json`); `*_APP_CHECK_ENFORCED` flags in `marketing_leads.ts`/`sms_rate_limit.ts`/`mfa_enrollment.ts`/`parent_linking.ts`/`impersonation.ts`/`school_resolution.ts`; `firestore.rules:749-780` (onboarding), `:236-238`+`:284-295` (tenant + self-admin).
- **Fix:**
  - [~] Activate App Check in release builds; watch metrics; then enforce on Firestore, Storage, Auth and every callable that supports it. Make the release build fail if the flag is omitted. *(Registered Android and iOS debug-provider canaries produced valid traffic; the three audio callables also passed a temporary enforced confirm/play/delete canary plus missing-token denial. Release mobile build targets now fail unless `LUMI_APP_CHECK_ENABLED=true`, and sensitive code/SMS clients request limited-use tokens. Play Integrity/App Attest and broader enforcement remain.)*
  - [x] Put public forms behind rate-limited callables (IP/device/email buckets, bot protection, payload limits, server timestamps). *(Demo requests were already migrated; school/student code verification now uses durable hashed per-IP/per-UID limits with strict unknown-IP fallback. SMS has its own server-side per-number quota. Public payloads remain size/schema bounded and server timestamped.)*
  - [x] Use high-entropy, short-lived join codes with attempt limits. *(All generation paths use cryptographic randomness; school/direct-parent codes default to 30 days, co-parent invites to seven, requested lifetimes are capped, active production credentials were migrated, and exact-code callables are rate-limited. School-code max usage is atomically consumed with an idempotent UID receipt.)*
  - [x] Fail **closed** for security gates, with an explicit operational override instead of silent fallback. *(Missing SMS config defaults to enabled/five per day; internal errors deny with `unavailable`. Only explicit `emergencyFailOpen: true` bypasses the gate, and production is enabled with that override false.)*
- **Verify:**
  - [~] Test: missing/invalid App Check token is denied after rollout. *(A live missing-token audio call returned 401 while enforcement was temporarily enabled, and registered physical iOS/Android debug traffic passed. Repeat with store attestation and malformed/expired tokens during the final rollout.)*
  - [~] Console: App Check enforced for Firestore/Storage/Auth/Functions; enforcement metrics healthy. *(The callable canary was healthy, but enforcement was deliberately restored off until store-signed clients exist and older traffic is migrated.)*

### `[x]` P1-2 · Real end-to-end in-app account/student deletion
- **Severity:** High (compliance + store-blocking)
- **Why:** `deleteAccount()` deletes only top-level `/users/{uid}` + Auth (memberships live under `/schools/.../users|parents`) and has **no call site**. `deleteStudentWithCascade` / `processPendingUserDeletions` don't cover the full log/comment/audio/class/notification footprint. Apple + Google require in-app deletion; the privacy policy promises deletion/de-identification on request.
- **Where:** `lib/services/firebase_service.dart:238-252`; `functions/src/index.ts` (`deleteStudentWithCascade`, `processPendingUserDeletions`); policy `school-admin-web/src/app/legal/privacy/page.tsx:147-165`.
- **Fix:**
  - [x] Build one server-owned, idempotent deletion workflow with a data inventory + visible job status: Auth, every membership/index, children + parent links, logs, nested comments, notifications/tokens, offline/widget data, audit-safe tombstones, Storage objects. *(The local/offline/widget caches are cleared through the existing shared sign-out flow after account completion; student deletion is server-owned and invalidates the deleted remote record.)*
  - [x] Add an in-app request/initiation surface (settings screen). *(Shared Account screen linked from both parent and teacher Settings; exact `DELETE` confirmation for accounts, plus exact full student name for student data. Stale sessions now reauthenticate inline with password, phone/SMS-MFA or linked Apple/Google provider and retry only after a same-UID token refresh; the server still enforces recent `auth_time`.)*
  - [x] Document legal retention exceptions, authorisation, school coordination, completion evidence. *(Parent account deletion de-identifies rather than destroys the school's core child reading event; assigned teacher/school-admin scope is re-checked server-side; minimal job receipts auto-purge after 90 days.)*
- **Verify:**
  - [x] Test: deletion removes all intended docs, nested comments, indexes, tokens and objects, and is safe to retry. *(Dedicated Firestore/Auth/Storage emulator integrations 2/2; deterministic/due/status helpers and index/TTL regressions pass; rules 136/136; Flutter deletion service/UI 7/7. A synthetic production iPhone fixture proved recoverable student deletion plus first-attempt account deletion, complete Auth/Firestore/Storage cascades, counter repair, deidentification, minimal receipts and final fixture cleanup.)*

### `[~]` P1-3 · Analytics privacy-by-default + accurate policy
- **Severity:** High (compliance)
- **Why:** `main.dart:144-173` starts Analytics + Crashlytics before UI; `analytics_service.dart:23-37` auto-enables in prod; `:43-108` links events to the Firebase UID and records child reading attributes (feeling, book count, minutes, badges, streaks) with no consent/opt-out. Policy calls this "aggregate" (`.../privacy/page.tsx:170-174`) — inaccurate while events are UID-linked.
- **Where:** as above.
- **Fix:**
  - [~] Default child-related Analytics + voice collection **off**; document necessity; obtain school/parent authority; support withdrawal. *(Analytics/Crashlytics default off with separate adult withdrawal controls. Per-school voice settings default off and recording remains an optional parent action. First enable now requires versioned school authority, family-notice/opt-out declarations and a retention choice; the one legacy-enabled demo school has no current evidence and therefore fails closed rather than being grandfathered. Each school must still perform the declared real-world notice/authority process.)*
  - [x] Remove direct UID linkage + detailed child attributes unless demonstrably necessary. *(No account UID/role is attached; detailed reading, badge, streak, error-reason and export-count parameters were removed.)*
  - [~] Verify actual Firebase SDK network traffic vs App Store/Play privacy labels (`docs/app-store/app-privacy-labels.md`). If using Apple Kids Category, apply its third-party-analytics restrictions. *(Native defaults and the built Android manifest are verified; repository labels/policy are aligned and conservative. A physical iPhone release-mode capture proves Analytics/Installations absent before opt-in, present after opt-in and absent again after withdrawal. The live store questionnaires and an opted-in synthetic crash payload remain.)*
  - [~] Before any AI/LLM analytics: complete a PIA (purpose, fields, training use, retention, subprocessors, offshore access, deletion, AU residency). *(A working PIA and AU STT evidence exist in `docs/AI_EVALUATION_PLAN.md`; legal/school approval and provider contractual gates remain, and production AI is disabled.)*
- **Verify:**
  - [x] Runtime capture confirms no child data is sent to optional diagnostics before consent; repository labels/policy match observed traffic. *(Physical iPhone cold-start/opt-in/withdrawal evidence captured 2026-07-17; raw captures deleted after endpoint extraction. Live App Store/Play questionnaire entry remains under the partial item above.)*

### `[x]` P1-4 · Offline: server receipt time, validated local dates, conflict policy
- **Severity:** Medium
- **Why:** The original implementation used client time for audit fields, lacked date bounds, silently dropped failed queue items and treated a school-local date as UTC during incremental removals. Those paths are repaired. The remaining risk is product-level concurrent parent/teacher edit policy plus real-device reconnect UX.
- **Where:** `lib/services/reading_log_service.dart`; `functions/src/stats_aggregation.ts:324-335`.
- **Fix:**
  - [x] Store a trusted `receivedAt` server timestamp; keep a separate validated local reading date with an allowed backdate/future-skew window. *(`createdAt` is the server receipt timestamp and must equal `request.time`; the user-selected `date` is separately bounded to 366 days back / 1 day forward.)*
  - [x] Define which fields may be edited by whom; use idempotency IDs; surface rejected queued writes. *(Rules enforce identity/system-field immutability and content allowlists; random 128-bit log IDs make replays idempotent; persistent denials park and surface in service status. Reading logs are create-once: when an offline queue finds an existing ID, the server record always wins and refreshes the local cache regardless of device timestamps. Feelings/comments retain separate field-specific merge queues.)*
  - [x] Convert school-local day boundaries to UTC using the school timezone; add DST regression tests. *(Exclusive UTC bounds now cover 23/25-hour Melbourne DST days and Sydney summer time.)*
- **Verify:**
  - [x] Tests: dates around Melbourne/Sydney midnight + DST preserve the correct local streak; offline write made before access revocation is rejected and clearly surfaced after reconnect. *(Date/stats unit tests, Firestore revocation denials **147/147**, offline parking and the future-device-clock/server-wins regression pass. The physical iPhone airplane-mode → server access suspension → reconnect scenario retained the item visibly, returned permission denied and left the authoritative server count at 50; after access restoration the same queued ID reached the server and moved the count to 51. Parent-safe errors, always-visible pending state and immediate explicit recovery are covered by the focused **40/40** offline tests.)*

### `[x]` P1-5 · Confirm incremental-aggregation flags + backfill before load
- **Severity:** Medium (cost/correctness)
- **Why:** Config defaults to legacy/full recomputation when flags are missing; legacy triggers can query *all* logs for a student/class after each write.
- **Where:** `functions/src/stats_aggregation.ts` (flag `platformConfig/incrementalAggregation`).
- **Fix:**
  - [x] Confirm prod flags + backfill are enabled before load. *(Both production flags are true. Seed fields were present on all 52 students and 14 classes checked; a controlled full reconcile completed after repairing the compound-query batch bug.)*
- **Verify:**
  - [x] Console/flag read shows incremental on; spot-check a write triggers incremental, not full recompute. *(Production flag read is true/true; each hot-path function processed 1,089 event deliveries with HTTP 200 and zero error entries after enablement; controlled reconcile processed 52 students/14 classes with zero errors and null completion cursors.)*

### `[x]` P1-6 · Lock down self-service tenant / parent / onboarding creation
- **Severity:** Medium
- **Why:** A signed-in user can self-create an empty parent membership for any known school ID (`firestore.rules:341-366`) → school-member reads (school doc, books) *before* a valid child-link. The anonymous `schoolOnboarding` rule accepts arbitrary extra fields + user timestamps and its update rule permits broad mutation.
- **Where:** `firestore.rules:341-366` (parent self-create), `:749-780` (onboarding).
- **Fix:**
  - [x] Create parent membership only inside the server-side verified linking transaction.
  - [x] Retire the direct-client onboarding path; use the validated/rate-limited server API only. *(Demo enquiries use `submitDemoRequest`; it validates and bounds the payload, writes the timestamp server-side, applies durable IP/email rate limits, and supports staged App Check enforcement.)*
- **Verify:**
  - [x] Test: signed-in user cannot self-create a parent membership without a verified link; all direct client reads/writes to onboarding are denied. *(Firestore Emulator 131/131; onboarding/callable client migration 28/28; Functions 113/113.)*

### `[~]` P1-7 · Secret scan + rotate demo/review defaults
- **Severity:** Medium
- **Why:** The initial audit found an active old-project admin credential on disk, incompletely restricted public client keys and missing artifact evidence. Demo/review credentials also needed to fail closed instead of falling back to checked-in passwords.
- **Where:** `scripts/seed_demo_school.js:151` + review-account script; the now-removed `school-admin-web/service-account.json`; Firebase API Keys/IAM consoles.
- **Fix:**
  - [~] Run a history-aware secret scanner (e.g. Gitleaks) in CI; scan built APK/IPA/web artifacts, not just source. *(Gitleaks 8.30.1 rescanned 612 commits: clean. A pinned redacted GitHub workflow and reusable `scripts/security/scan-secrets.sh` are active; the script now extracts APK/AAB/IPA packages so a zero-byte compressed-file scan cannot be accepted. Fresh App-Check-enabled release APK (1,105 extracted files), unsigned iOS `Runner.app`, portal server output and portal static output contain no private key, service-account credential or demo password. A final Play-signed AAB and signed IPA are not yet available to scan.)*
  - [x] Remove checked-in demo/review password defaults. *(Both seed paths require a password-manager-supplied `DEMO_PASSWORD` of at least 16 characters; the demo workflow rotates shared passwords daily. Release-artifact scans found no provisioning password.)*
  - [x] Rotate the `lumi-kakakids` key; move the portals to Application Default Credentials. *(Both portals now use their attached Cloud Run identities and do not load repository/build JSON credentials. The exposed old-project key and the duplicated current-project Admin key were disabled, deleted from IAM and removed locally; fingerprint scans found no cached copy. The super-admin dashboard remained healthy after current-key revocation.)*
  - [x] Replace the admin GitHub Actions JSON deploy key with keyless Workload Identity Federation. *(Pool/provider and repository/main-only condition are active. Main run `29492117424` authenticated with OIDC/WIF and deployed successfully; the legacy GitHub secret and service-account key were then deleted, leaving zero user-managed deploy keys. Live admin login remains HTTP 200.)*
  - [~] Restrict Firebase browser/iOS/Android API keys by bundle ID/package/signing cert/API/quota in GCP. *(Browser, iOS and the currently debug-signed Android build are platform-restricted and retain the 27-service Firebase API allowlist. The separate old Books key is target-restricted to Books only. Identity Toolkit verification-code sends are capped at a granted 500/day, and enabled Auth-quota/blocked-SMS alert policies notify both security recipients. Add the Play App Signing SHA-1 before closing.)*
- **Verify:**
  - [~] Gitleaks CI green on history + artifacts; portal boots without a repository JSON key. *(Local history and available deploy-artifact scans are green; PR #409's required history scan passed, its WIF deploy passed, and the admin login returns 200 after permanent key revocation. Live Identity Toolkit probes accept registered web/iOS/Android identities and reject unregistered ones with 403; the 500/day verification-code cap and Auth/SMS alerts are live. Await the signed IPA/store scan, Play App Signing registration and signed-device smoke tests.)*

### `[x]` P1-8 · Misc session / legacy-doc hardening
- **Severity:** Medium
- **Fix:**
  - [x] Portal session: `school-admin-web/src/lib/auth/session.ts` keeps a valid JWT on Firestore read failure — make privileged mutable routes fail **closed** (a narrow cached read-only fallback may be OK). *(Every ordinary mutation route opts into `requireMutable`; read-only JWT fallback remains available.)*
  - [x] Top-level `/users/{uid}` lets a user write arbitrary fields incl. `role` (`firestore.rules:742-746`) — not used for school authz today, but make it server-owned / limit self-writable fields so it can't become a future escalation source. *(Create/delete denied; legacy self-update is limited to validated, server-timestamped terms acceptance.)*
  - [x] `devAccessEmails/{sha256(email)}` is a probe-able email oracle (`firestore.rules:926-928`) — restrict the callable result to the caller's own token email. *(All direct client access denied; `checkDevAccess` accepts no candidate email/hash and derives it from the verified Auth token.)*
- **Verify:**
  - [x] Test: deactivated/demoted user cannot use an old portal/app session; `/users` self-write can't set a privileged field. *(Portal session policy 2/2; Firestore Emulator 133/133; mobile callable migration 2/2; Functions 113/113; portal production build passes.)*

### `[x]` P1-9 · Prevent Android backup/device-transfer restore of sessions and child data
- **Severity:** High (privacy + session persistence)
- **Why:** A physical Android 16 reinstall restored an earlier authenticated Lumi state despite `allowBackup=false`, which some Android 12+ manufacturers may ignore for device-to-device migration. That can move Firebase Auth state and cached child data onto a replacement device without a fresh Lumi sign-in.
- **Where:** `android/app/src/main/AndroidManifest.xml`; `android/app/src/main/res/xml/data_extraction_rules.xml`.
- **Fix:**
  - [x] Keep legacy cloud backup disabled and add Android 12+ extraction rules that exclude all app-data domains from both cloud backup and device transfer.
  - [x] Clear the unexpectedly restored real-account state immediately; revoke the local App Check debug token used in that interrupted canary.
- **Verify:**
  - [x] Debug APK builds; packaged manifest resolves `android:dataExtractionRules`; `aapt2` shows all cloud/device-transfer exclusions; regression test 1/1 passes; a full physical uninstall/reinstall restores no Firebase Auth preference/database and makes no authenticated school query before sign-in.

---

## P2 — First production month (retain evidence)

- `[x]` **P2-1 Budgets + dashboards.** Cloud Billing budgets/alerts, per-service anomaly monitoring, quotas, security/cost dashboards. Alert on reads/writes/egress/functions/SMS/Storage. *(Project-scoped AUD 100 monthly budget is active. Thirteen enabled high-signal policies cover Cloud Run failures/egress, Firestore reads/writes/rules errors, App Check, Auth quota/SMS, Firebase Storage egress/footprint, audio security failures, deletion failures and impersonation anomalies, all attached to both security recipients. Firestore read/write Data Access logging, the live security/cost dashboard and idempotent checked-in configs are complete. Nicholas Plevritis confirmed synthetic incident `0.oabm7h2yj055` arrived in both security inboxes on 17 July 2026.)*
- `[x]` **P2-2 Backups + restore drill.** Enable Firestore PITR and/or scheduled exports with retention; perform a timed restore drill; record recovery time + data loss. *(Seven-day PITR and database deletion protection are live. A five-minute-old snapshot cloned in 12m59s; 5 schools/52 students/14 classes/714 reading logs exactly matched source, then the temporary AU database was deleted.)*
- `[~]` **P2-3 Privacy program.** Complete a Privacy Impact Assessment, vendor/data-flow register, and a breach tabletop exercise. *(Working PIA, vendor/data-flow register, breach plan and a documented cross-school Rules tabletop are complete. First-opt-in audio authority/notice/retention evidence is technically enforced. Support/alert delivery is verified, and Nicholas Plevritis plus authorised backup Kylie Plevritis hold the operational incident roles. External privacy/legal leadership, owner/counsel PIA approval, school emergency contacts and final APP 8 legal decisions remain, so this is not yet a legal/commercial sign-off.)*
- `[x]` **P2-4 Cost/scale.** Load-test dashboards + rules billing at 30/100/1,000-student schools; paginate/limit queries; replace global fallback scans (`resolveUserSchoolByUid` unbounded collection-group fallback) with a server-owned UID index; verify listeners are disposed; materialise dashboard summaries; avoid hot counters + sequential IDs. *(Complete. The UID index, listener audit, random IDs, incremental aggregates, 600-second probe and 30-record history pagination are verified. Server-only eight-shard class/day summaries replaced raw multi-week calendar/home reads while preserving exact active-reader counts. The post-migration real-Rules profile reads 56 summary shards/week and at most 672/12 weeks at 30, 100 and 1,000 students, versus 2,520/8,400/84,000 raw documents. Rules/indexes, duplicate/update/delete and corruption-repair tests pass; production backfill verified 697 eligible logs and 12,857 minutes exactly across 459 shards. The retained optional current-week detailed query is bounded and intentionally supplies attributes excluded from the minimised summary. See `docs/security/COST_SCALE_AUDIT_2026-07-17.md`.)*
- `[~]` **P2-5 Device + error-path testing.** Old iPads / low-end Android; offline/DST/date-tamper; account revocation; force-upgrade (make it fail into a support mode when version config is unavailable + configure store URLs); no-network, expired-session, empty-class, denied-camera, book-not-found/manual-entry paths on real devices. *(The force-update evaluator/UI and automated failure-path batches pass; a deliberately under-configured direct iOS release physically failed closed into support mode, and the properly configured release continued normally. Samsung A57/Android 16 direct-install, iPhone audio/consent, the complete offline access-revocation/recovery lifecycle and the portal-only admin session/sign-out flow passed. The 17 July physical iPhone ISBN batch passed known-book camera scan, camera denial, correct manual lookup, invalid input retention and an unresolved-book add flow. Two discovered UX gaps were repaired and retested: a clear `Book not found` add/cancel prompt now precedes capture, catalogue outages are distinguished from definite misses, and iOS captures one cover photo before Lumi's crop/rotate review. Both cancel and positive paths passed, and all synthetic Firestore/Storage book artefacts were removed. Physical student and account deletion also passed. The student path exposed a missing `notifications.studentIds` collection-group index and raw `INTERNAL` UI copy; both were repaired without losing the recoverable idempotent job, and scheduled attempt 2 completed the full cascade. Account preflight found and repaired missing group indexes for `comments.authorId` and `deletionRequests.requestedBy`; after both were `READY`, the physical account deletion completed on attempt 1 and returned to Login. Production verification proved Auth, user/membership/index, feedback and notification deletion; school-record deidentification; teacher/student counters restored to 2/16; and both 90-day receipts stripped of raw subject identifiers. Four intentionally preserved synthetic school-owned records were then removed, leaving only the minimal receipts. Functions pass 144/144. Remaining: genuinely old/low-end hardware, the iOS listing URL and a store-signed update canary.)*
- `[x]` **P2-6 Recurring reviews.** Schedule dependency, rules and privacy reviews for every release that changes auth, child data, Storage, analytics or vendors. *(The PR template and `docs/privacy/RELEASE_PRIVACY_SECURITY_REVIEW.md` define the release gate and evidence; `.github/workflows/security-review.yml` schedules weekly/manual Functions, Rules and production dependency checks; IAM, vendor, retention, PIA, tabletop, store-label and alert-delivery cadences are assigned.)*

---

## Verify outside the repo (console / IAM / ops — can't be proven from code)

- `[x]` Deployed `firestore.rules` + `storage.rules` **exactly match** the reviewed source. *(17 July post-audio-gate verification: Firestore local and downloaded-active SHA-256 both `6c3195e20f648eec43322478b5402697a12cc1b144bc9b24db80ada510a5512e`, active ruleset `ebc3bae6-9919-4ca7-b71a-fcbfbb8fa283`; Storage local and downloaded-active SHA-256 both `d99656d8e146d7ecd881fe4c1d1a35b0616f0a6159e97dc230a09b539ee975f4`, active ruleset `0033c14a-0038-478c-8043-868bd42746b4`.)*
- `[~]` App Check registration + metrics + enforcement (Firestore, Storage, Auth, Functions). *(Registered physical iOS/Android debug canaries produced valid traffic and the audio callables passed a temporary enforced run, but Firestore/Storage/Auth remain correctly unenforced until store-attested Play Integrity/App Attest traffic and legacy-client migration are proven.)*
- `[~]` Firestore/Storage/Auth/Functions resource locations + cross-border support/data flows are AU. *(Firestore, user-content Storage, all 77 live workloads, current Function sources, future ordinary logs and the three active application secrets are in `australia-southeast1`; the former global secret resources were deleted after a verified cutover. This cannot be closed as “all AU”: Firebase Authentication is US-only; FCM/App Check/Analytics/Crashlytics and required audit logs are global; SendGrid is cross-border; and a legacy unreferenced US Functions source bucket holds historical code archives. The technical inventory and public vendor evidence are complete; private account acceptance and owner/counsel APP 8 approval remain.)*
- `[x]` Least-privilege IAM + service-account permissions. *(All Functions and both portals use dedicated keyless runtimes; validator isolation remains. App Engine/Compute `Editor`, stale runtime grants, project-wide deploy actAs and project-wide deploy Secret Accessor were removed. Capability canaries, a fresh build/deploy and Scheduler run passed after removal; definitions and rollback live under `infra/iam`.)*
- `[~]` Google API-key restrictions, Firebase Auth quotas, SMS abuse alerts. *(Browser/iOS/current Android identities and API targets are restricted and live negative tests pass. Identity Toolkit verification-code sends have an effective 500/day project cap; Auth-quota and blocked-SMS alert policies are enabled for both recipients. Add the Play signing SHA-1 after store enrolment.)*
- `[x]` Cloud Billing budgets/alerts + per-service anomaly monitoring. *(AUD 100 project-only monthly alert budget, 13 enabled anomaly policies and the security/cost dashboard are live from checked-in configs. Nicholas confirmed synthetic incident `0.oabm7h2yj055` reached both named inboxes on 17 July 2026.)*
- `[x]` Firestore PITR/scheduled backups + retention + a successful timed restore drill. *(Seven-day PITR + deletion protection enabled; 12m59s point-in-time clone drill matched sampled production counts and was cleaned up.)*
- `[~]` Production logs/alerts for denied writes, function errors, deletion jobs, audio access, auth spikes, spend. *(Thirteen enabled policies and Firestore DATA_READ/DATA_WRITE audit logging cover the available high-signal paths. Both notification inboxes are confirmed; review Data Access log volume after one week. Direct denied-write event detail is limited by Firebase's managed Rules telemetry.)*
- `[x]` Stable privacy/terms/support URLs + a monitored support mailbox. *(All three stable live URLs return HTTP 200; the owner confirmed on 17 July 2026 that `support@lumi-reading.com` is monitored daily and protected by MFA. Provider/delegate/retention details remain a vendor-register governance action, not a mailbox-availability blocker.)*
- `[~]` Written, exercised data-breach plan aligned with the OAIC Notifiable Data Breaches scheme. *(Working plan and 17 July cross-school tabletop are documented. Nicholas Plevritis is primary for all operational roles and Kylie Plevritis is authorised backup; external Australian privacy/legal counsel remains unappointed. Store a protected copy outside production and close the school-contact/legal governance gaps.)*
- `[ ]` App Store / Play privacy disclosures match runtime SDK traffic.

---

## New negative-test matrix (each line = one automated test that must pass)

- `[x]` Class-A teacher cannot read/write Class-B student, log, comment, allocation, group, AI eval or audio. *(Firestore 145/145; audio handler integration denies unassigned teachers.)*
- `[x]` Parent-A cannot read/write Parent-B's child, log, comment or audio. *(The consolidated Firestore matrix includes own-record positive controls plus cross-parent membership/child/log/comment get, targeted and unfiltered list, log update/delete and forged comment denials. Storage uses a legitimate unrelated parent and denies a forged audio upload. Firestore 151/151; Storage 13/13.)*
- `[x]` School-X cannot access School-Y, including through an injected audio path. *(Firestore/Storage, handler integration and real callable HTTP boundary all pass.)*
- `[x]` Parent cannot change `studentId`, `classId`, `parentId`, school, author, created time, status, validation, stats or audio fields.
- `[x]` Teacher proxy log denied when child and class don't match.
- `[x]` Comment denied when its child/parent doesn't match the containing log.
- `[x]` Authenticated outsider cannot upload/overwrite covers or any school's audio.
- `[x]` Disabled-audio flag blocks upload, URL minting and processing server-side. *(Storage rule and handler integration tests pass.)*
- `[x]` Expired/deactivated teacher cannot use an old portal/app session. *(Portal mutable-session policy and Firestore role/access negative tests pass; read-only cached fallback cannot mutate.)*
- `[~]` App Check missing/invalid token is denied after rollout. *(Missing token was denied in both the local integration and a temporarily enforced production audio callable; registered iOS/Android debug-provider traffic passed. Store-attested and malformed/expired-token checks remain for rollout.)*
- `[x]` Offline write made before access revocation is rejected and clearly surfaced after reconnect. *(Physical iPhone demo test: the queued entry stayed visible, Firestore denied it after access suspension, the server count did not change, and it later synced exactly once after authoritative access restoration.)*
- `[x]` Deletion removes all intended documents, nested comments, indexes, tokens and objects, and is safe to retry. *(Dedicated Firestore/Auth/Storage cascade integrations 2/2 plus deterministic retry/status helpers 3/3.)*
- `[x]` Dates around Melbourne/Sydney midnight + DST changes preserve the correct local streak. *(Functions regressions cover Melbourne 23/25-hour DST days, Sydney summer time and invalid-timezone fallback.)*

---

## Baseline test/tool results (15 Jul 2026 — reference)

| Check | Result | Interpretation |
|---|---:|---|
| Firestore Emulator rules | 125 passed | Baseline only; cross-class/mass-assignment/comment-binding negatives absent. |
| Storage Emulator rules | 6 passed | Cover audio shape + direct-read denial; assert broad upload, omit cover/cross-tenant. |
| Cloud Functions unit/build | 109 passed | Helper correctness only; audio-path exploit not covered. |
| Flutter offline/log tests | 40 passed | Good queue coverage; clock/DST/two-device conflict cases remain. |
| Flutter static analysis | 141 info lints | No error/warning; informational debt. |
| Functions prod dep audit | 22 advisories | 2 crit, 7 high, 12 mod, 1 low. |
| Portal dep audit | Inconclusive | pnpm endpoint HTTP 410; re-run with a working tool. |
| Secret pattern scan (tree) | No private key | Demo defaults + public client keys remain; history/artifacts unscanned. |

---

## Australian children's-privacy context (draft OAIC Children's Online Privacy Code)

Exposure draft released 31 Mar 2026; expressly covers examples like online school-management systems that monitor student performance; calls for strictly-necessary processing + high-privacy-by-default; requires a PIA before significant child-data changes; proposes prompt destruction (normally ≤30 days). Still a draft — details/commencement may change. **Don't assume the small-business turnover exemption applies** (Act has exceptions; school + Victorian public-sector contracts may bind regardless). Get Australian privacy advice for Lumi's exact entity/customers/data flows.

Before launch Lumi should have: a data inventory + child-data purpose/necessity table; high-privacy defaults (voice + analytics off by default); a published PIA/register process; working access/correction/deletion/destruction workflows with deadlines; school-facing answers on Firebase subprocessors, overseas processing, support access, retention, deletion, encryption, AU resource locations; SDK/runtime network verification matching policy + store labels; a child-safe support/escalation procedure with verified authority for parent/school requests.

---

## Final assessment

Lumi's foundation is recoverable and several strong controls are present, but the current authorisation model stops at the school boundary — too broad for child data. The fastest safe route: make **`school → class → child → record/object`** the common binding everywhere, keep client-editable fields intentionally small, derive privileged object paths server-side, and prove the denial cases in the emulator. Land deletion, privacy-by-default analytics and the operational controls before launch — not as post-launch paperwork.
